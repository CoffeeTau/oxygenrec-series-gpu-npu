# OxygenREC-v1 代码阅读与数据流指南

> 目标：帮助第一次审阅本项目的人，从真实代码理解数据如何变成 SID、如何进入
> Encoder-Decoder，以及 Contextual Reasoning、Q2I、IGR 分别插在什么位置。

## 1. 先看结论：目前完成到哪里

| 模块 | 当前状态 | 已验证内容 | 仍需注意的边界 |
|---|---|---|---|
| Encoder-Decoder GR | 已完成 GPU 方法复现 | weighted NTP、teacher forcing、toy overfit、10万样本训练、checkpoint、greedy、PrefixTrie beam | 是小型 Dense 自实现，不是论文私有规模与参数 |
| Contextual Reasoning Instruction | 已完成生成与结构接入 | Qwen3-4B 真实生成 JSON Reasoning/Plan；schema、证据边界和人工 review 通过 | 文本仍有商品/类目混淆；尚未 SFT |
| Q2I semantic alignment | 已完成结构与训练验证 | query/item adapter、cosine alignment、variance/decorrelation、与 NTP 联合 loss | 公开代理数据效果不稳定；不能外推论文收益 |
| IGR | 基础链完成 | query 检索冻结长历史、top-k 拼入 Encoder、真实长历史 paired 验证 | Qwen Plan 已接入 `forward()`，但尚未透传到 `generate()/beam_search()` |
| SA-GCPO | 代理结构完成 | reward、rollout、group objective 与 checkpoint 更新通过 | 私有 reward service 不可得，真实 Qwen RL 尚未做 |

因此，你的理解基本正确。需要特别补充两点：

1. Encoder-Decoder GR 已经完成，而且它是整个项目最核心的 Fast Model 骨架；
2. “Qwen Plan→IGR”目前通过的是检索诊断/Encoder forward，不等于已经改变最终 beam 推荐。

## 2. 推荐阅读顺序

不要一开始就读训练脚本或 Qwen。建议按下面顺序：

1. `src/oxygenrec/data/events.py`
   - 看原始 CSV 一行如何变成 `InteractionEvent`。
2. `src/oxygenrec/data/temporal.py`
   - 看如何按全局时间切分，以及如何保证 history 严格早于 target。
3. `src/oxygenrec/sid.py`
   - 理解 `SemanticID`、`SIDRegistry`、碰撞和 `PrefixTrie`。
4. `src/oxygenrec/data/model_inputs.py`
   - 看 item ID 如何变成 `[B,H,L]` 的 SID Tensor；理解短历史/长历史切分。
5. `src/oxygenrec/model.py`
   - 先读 `forward()`，再顺着 `_instruction_prompt → _augment_history → _encode → _decode`。
6. `scripts/train_retailrocket.py`
   - 看上述组件怎样被组织为训练、验证和 checkpoint。
7. `src/oxygenrec/llm_reasoning.py`
   - 看真实 Qwen 如何生成结构化 Reasoning。
8. `src/oxygenrec/retrieval_planning.py`
   - 看自然语言 Plan 如何经过白名单编译，变成有界检索控制。
9. `src/oxygenrec/evaluation.py`
   - 看 SID 如何还原为商品集合，并计算 HR/MRR/NDCG。

早期 hash 文本代理位于 `src/oxygenrec/instructions.py`。它已经被判定为效果失败并冻结，
只需理解它曾用于打通“文本实际进入模型”的接口，不要把它当成当前主路线。

## 3. 一条样本是怎样产生的

### 3.1 原始事件

RetailRocket 的一行：

```text
timestamp, visitorid, event, itemid, transactionid
```

在 `retailrocket_event_from_row()` 中被转换为：

```text
InteractionEvent(
    timestamp_ms,
    source_row,
    user_id,
    item_id,
    behavior,
    transaction_id,
)
```

`source_row` 只用于稳定排序。同一毫秒的事件不会互相作为历史，因为数据没有给出
它们的真实因果顺序。

### 3.2 时间样本

`build_next_item_samples()` 按用户和时间遍历事件：

```text
严格更早的事件 -> history
当前事件       -> target
```

validation/test target 可以使用更早 split 中的历史，但 target 本身及同时间事件绝不
进入 history。由此得到：

```text
NextItemSample(
    split,
    user_id,
    history=(event_1, ..., event_n),
    target=event_n+1,
)
```

### 3.3 item ID 变成 SID

`SIDRegistry` 保存：

```text
item_id -> SemanticID([code_0, code_1, code_2])
```

例如：

```text
item 4808 -> [185, 73, 113]
```

三层 SID 是生成模型的三个 token。多个商品可能碰撞到同一个 SID，所以反向映射是：

```text
SID -> (item_a, item_b, ...)
```

评测时只要目标商品存在于这个显式集合中，就算该 SID 命中。

## 4. 两种模型输入

### 4.1 普通 Encoder-Decoder 输入

`build_sid_model_batch()` 产生：

```text
history_sids         [B, H, L]
history_padding_mask [B, H]
history_behavior_ids [B, H]
target_sids          [B, L]
```

其中：

- `B`：batch size；
- `H`：短历史长度；
- `L`：SID 层数，当前固定为 3；
- padding 放在左侧，最近事件仍位于序列尾部。

### 4.2 IGR 的长短历史输入

`build_long_short_sid_model_batch()` 把历史切成：

```text
较早的 long history -> IGR 候选池 [B, H_long, L]
最近的 short history -> Encoder 主输入 [B, H_short, L]
```

两者不重叠。IGR 从 long history 取回 K 个 SID，最终 Encoder 输入变为：

```text
[short history ; retrieved K items] -> [B, H_short + K, L]
```

## 5. Encoder-Decoder GR 主链

核心入口是 `OxygenRECModel.forward()`。

### 5.1 Instruction 与 query

`_instruction_prompt()` 构造：

```text
scenario prompt       [B,D]
reasoning prompt      [B,D]
query                 [B,Q]
```

`reasoning prompt` 可以来自：

- learned instruction embedding；
- hash/LLM hidden-state 经 adapter 后的特征；
- 短历史 context pooling；
- 上述信息的组合。

`query` 由 `[scenario; reasoning]` 拼接后经过 MLP，并做 L2 normalize。它同时服务：

- IGR：与长历史商品向量做 cosine top-k；
- Q2I：与目标商品向量做语义对齐。

### 5.2 IGR

`_augment_history()` 的计算是：

```text
long SID [B,H_long,L]
  -> 三层 SID embedding 求和 [B,H_long,D]
  -> item_adapter + normalize [B,H_long,Q]

query [B,Q] · long vectors [B,H_long,Q]
  -> semantic_scores [B,H_long]
  -> top-k indices [B,K]
  -> gather SID [B,K,L]
  -> 拼入短历史
```

长历史商品向量被 `detach()`，因此检索分支不会反向修改 SID embedding 和 item adapter。

如果传入 Qwen Retrieval Plan，`execute_retrieval_plan()` 会在 semantic score 上增加
小幅、固定、有边界的行为/时间/重复/多样性控制。自由文本不参与打分。

### 5.3 Encoder

`_encode()` 对每个历史商品计算：

```text
position embedding
+ SID level-0 embedding
+ SID level-1 embedding
+ SID level-2 embedding
+ 可选 behavior embedding
```

得到 `[B,H(+K),D]`，送入 `nn.TransformerEncoder`，输出同形状 memory。

### 5.4 Decoder

`_decode()` 的 token 布局为：

```text
[scenario, reasoning, BOS, SID-level-0, SID-level-1]
```

训练时使用 teacher forcing：

- 预测 level 0 时只看 prompt/BOS；
- 预测 level 1 时额外看到真实 level 0；
- 预测 level 2 时额外看到真实 level 0、1。

三个 `prediction_heads` 分别输出：

```text
logits_0 [B,V]
logits_1 [B,V]
logits_2 [B,V]
```

`V` 是每层 SID vocabulary width。

### 5.5 Weighted NTP

`weighted_ntp_loss()` 对三层分别做交叉熵，再按 level weight 求归一化加权和：

```text
L_ntp = sum(w_l * CE(logits_l, target_l)) / sum(w_l)
```

这就是 Encoder-Decoder GR 的主要训练目标。

## 6. Q2I 在哪里

当 `q2i_weight > 0` 时，`forward()` 额外计算：

```text
query = normalize(query_adapter([scenario; reasoning]))
target = normalize(item_adapter(target SID embedding))
```

`q2i_alignment_loss()` 包含：

1. 负 cosine：拉近 query 与目标商品；
2. variance：防止表示塌缩；
3. decorrelation：降低 batch 内 query 过度相似。

联合损失为：

```text
L_total = L_ntp + q2i_weight * L_q2i
```

训练脚本每个 batch 都检查这个恒等式，防止日志字段与实际优化目标不一致。

## 7. Contextual Reasoning Instruction 在哪里

当前真实路线分为两步：

1. `build_behavior_prompt()`：只把 target 之前的历史计数、最近行为和重复商品数量写入 prompt；
2. `FrozenLLMReasoningGenerator.generate()`：让本地 Qwen3-4B 输出严格 JSON。

JSON 的核心可执行字段是：

```json
{
  "priority_behaviors": ["view", "addtocart"],
  "recency": "balanced",
  "prefer_repeated_items": true,
  "diversity": "medium"
}
```

`compile_retrieval_plan()` 会过滤历史中不存在的行为，并把文本枚举转换为执行对象。
`execute_retrieval_plan()` 才真正修改 IGR score。

注意：Qwen 输出的 `intent/evidence/retrieval_strategy` 用于审计，不直接进入数值打分。

## 8. 训练脚本怎么串起来

`scripts/train_retailrocket.py` 的主流程：

```text
load_retailrocket_events
  -> TemporalBoundaries（全局80/10/10时间切分）
  -> SIDRegistry（读取已拟合registry或频率bootstrap代理）
  -> build_next_item_samples
  -> tensor_batch
  -> OxygenRECModel.forward
  -> backward / optimizer.step
  -> beam_search
  -> evaluate_sid_ranking
  -> checkpoint + metrics.jsonl
```

`variant` 控制打开哪些模块：

| variant | 主要模块 |
|---|---|
| `base` | Encoder-Decoder + weighted NTP |
| `instruction` | base + scenario/trigger instruction |
| `q2i` | instruction + Q2I |
| `igr` | instruction + IGR |
| `igr_q2i` | instruction + IGR + Q2I |
| `igr_generic_q2i` | 去掉目标行为 scenario 的对照 |
| `igr_text_q2i` | 使用早期 hash 文本代理的对照，已冻结 |

## 9. 生成和评测

`generate()` 使用 PrefixTrie 做逐层贪心生成；`beam_search()` 保留多个合法前缀：

```text
空前缀
 -> 合法 level-0 code
 -> 合法 level-1 code
 -> 合法 level-2 code
 -> 完整 SID
```

`evaluate_sid_ranking()` 再通过 registry 把 SID 还原成商品集合，计算：

- HR@K / Recall@K；
- MRR；
- NDCG；
- legal SID rate。

## 10. 审阅代码时最值得关注的三个问题

1. 数据泄漏：任何 instruction、history、candidate 是否都严格早于 target？
2. 训练与推理一致性：训练中使用的 scenario/文本，在真实推理时是否可获得？
3. 结构通过与效果提升是否被混淆：CUDA work 只证明方法链可执行，不等于论文收益复现。

建议你先完整读完 `events.py → temporal.py → model_inputs.py → model.py`，然后带着
`forward()` 的五步结构，再读 Qwen 与检索计划代码。这样不会被大量实验脚本打断主线。
