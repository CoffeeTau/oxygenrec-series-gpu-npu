# OxygenREC 复现进度

更新时间：2026-08-28 11:16:53 CST
当前阶段：OxygenREC-v1 Qwen Plan最终生成闭环已完成GPU验收；进入Qwen LoRA SFT数据协议与最小训练闭环

详细的逐次实验输出、问题判断、解决方法与结论演变见：`复现实验日志.md`。

## 1. 项目边界

本项目的准确表述是：

> 先在GPU侧完整自实现OxygenREC-v1公开结构与方法 + 公开数据代理 + 定量测试 + 定性样例review。

当前结果不能称为京东生产模型、私有数据主表或线上收益的严格复现。论文未公开完整工业数据、商品特征、训练代码、checkpoint、reward service 和线上协议。

### 1.1 过渡项目的学习优先级

| OxygenREC内容 | 投入级别 | 与后续LLM/Agentic Search的交叉 |
|---|---:|---|
| Contextual Reasoning Instruction | 五星 | 意图分解、提示构造、推理过程与可解释轨迹 |
| IGR / Retrieval | 五星 | query构造、长上下文检索与retrieval-augmented search |
| SA-GCPO / RL | 五星 | group policy optimization、reward mapping、轨迹优劣与稳定训练 |
| v2 trajectory optimization | 五星 | 多步search trajectory、teacher/privileged feedback与策略改进 |
| Q2I semantic alignment | 四星 | query-document/item representation alignment与检索语义一致性 |
| SID / RQ、Encoder-Decoder GR | 二至三星 | 只掌握tokenization和受约束生成基础，不深挖参数 |
| 工业多场景推荐 | 二至三星 | 只验证条件控制接口，不复制私有场景工程 |

执行原则：推荐特有模块达到“GPU可前向/反向、边界清楚、一个样例可review”即停；主要工作量投入Reasoning、Retrieval、Alignment、RL和Trajectory。

状态标记：

- `[完成-实测]`：实现完成，并在目标服务器实际执行；
- `[完成-本地]`：实现完成，纯 Python 测试通过；
- `[代理-实测]`：公开数据替代方案已实际执行，但不是论文原始组件；
- `[待验证]`：代码已实现，但尚未在相应依赖或硬件上运行；
- `[未开始]`：尚未进入实现；
- `[阻塞]`：缺少数据、论文细节、硬件或外部依赖。

## 2. 总体进度

| 目标 | 当前判断 | 说明 |
|---|---:|---|
| 最小工程闭环 | 约 90% | 数据、代理 SID、模型、解码、指标、checkpoint 和 SA-GCPO rollout 已贯通 |
| OxygenREC-v1 关键论文方法 | 约 94% | Fast backbone、代理SID、Q2I/IGR、SA-GCPO结构已验；真实Qwen Reasoning→Plan→IGR→greedy/beam/rollout GPU链完成，剩余为SFT/RL训练层 |
| OxygenREC-v2 | 低于 5% | 只有接口设计预留，尚未实现行为条件预训练和后训练 |
| GPU/NPU 双平台 | `[暂缓]` | 对齐脚本保留；OxygenREC-v1 GPU验收完成前不进入NPU |
| 3B-A1B MoE 最终目标 | 0% | 必须在 dense 方法闭环之后启动 |

百分比只用于项目监控，不代表论文指标复现比例，也不用于对外成果声明。

## 3. 论文方法对照表

| 论文模块 | 当前状态 | 当前实现 | 主要缺口 |
|---|---|---|---|
| 数据与时间协议 | `[完成-本地]` | RetailRocket 流式读取、全局时间切分、同毫秒隔离、train-only vocabulary | 尚未冻结正式数据版本和最终 cutoff |
| 多模态商品编码 | `[完成-实测已冻结]` | 文本/图像token投影、modality embedding、残差Q-Former、MLP融合 | CUDA overfit与双模态影响通过；不再深挖推荐特有编码 |
| 三层 RQ-KMeans | `[代理-实测]` | PyTorch 分块 Lloyd RQ，三层 codebook，width 可配置 | 论文只披露 3层×8192；初始化和训练细节未知，当前 width 较小 |
| SID registry/collision | `[完成-本地]` | 版本化 item↔SID、碰撞保留、JSON 持久化 | 尚未冻结可用于正式实验的低碰撞 registry |
| Fast encoder-decoder | `[完成-实测]` | 小型 dense Transformer encoder-decoder | 规模、层数和工业特征与论文不同 |
| level-aware SID embedding | `[完成-实测]` | 三层独立 embedding，历史商品层内求和 | 尚未进行论文规模消融 |
| 三层 NTP heads/loss | `[完成-实测]` | 三个 level-specific heads、加权交叉熵 | 论文完整权重和训练配方未披露 |
| Prefix 约束解码 | `[完成-实测]` | greedy + deterministic reference beam search | 尚未做高性能批量 beam/KV cache |
| HR/Recall/MRR/NDCG | `[完成-本地]` | SID→item、碰撞显式命中、合法 SID 比例 | 需在正式 registry 和固定 test split 上报告 |
| Contextual Reasoning Instruction | `[真实LLM生成实测]` | Qwen3-4B生成JSON reasoning；白名单retrieval plan、证据限制与确定性编译器 | v2三例结构通过、内容部分通过；存在商品/类目混淆和过度解释；尚未SFT |
| Instruction fusion | `[完成-实测]` | scenario/trigger 与 reasoning 作为两个 decoder prompt token | 尚待真实 instruction 消融 |
| Q2I alignment loss | `[完成-实测]` | query/item adapter、归一化对齐、方差与去相关正则、联合 NTP | 合成可控对齐及RetailRocket alignment均通过；hash文本相对Generic无优势 |
| Instruction-Guided Retrieval | `[完成-实测]` | query cosine top-k；Qwen plan到行为/时间/重复/多样性有界控制并进入模型IGR；Plan已透传至greedy/beam/candidate log-prob | CUDA输出`planned_paths=3`且形状、有限分数、合法SID均通过；不作统计收益结论 |
| 多场景 instruction | `[完成-实测]` | scenario embedding 接口和可选 trigger SID 已实现 | RetailRocket 场景映射方案尚未定义 |
| Reward Mapping Service | `[代理-实测]` | format/relative/ranking/diversity公开代理已接入rollout | 私有统一ranking service不可得，ranking只能做代理 |
| SA-GCPO | `[完成-代理实测]` | 公式4-9、rollout和三种子held-out对照 | paired rank弱正向，HR收益不稳定；私有reward不可得 |
| v2 behavior instruction | `[完成-代理失败]` | behavior embedding、target strength和时间衰减控制流 | 5000条复评中总体及transaction均低于Base |
| v2 privileged distillation | `[未开始]` | 无 | 需要未来行为 teacher 和 entropy-aware 策略 |
| MoE | `[未开始]` | 无 | dense 闭环和分布式验证前不启动 |

## 4. 已完成基础设施

### 4.1 SID 与量化

- `[完成-本地]` `SemanticID`：三层 code 范围和层数校验；
- `[完成-本地]` `SIDRegistry`：版本化映射、碰撞保留、保存/加载；
- `[完成-本地]` `PrefixTrie`：合法前缀和 next-code 查询；
- `[完成-本地]` 纯 Python reference K-Means++、Lloyd K-Means、Residual K-Means；
- `[完成-实测]` PyTorch/CUDA 分块 Residual K-Means；
- `[完成-本地]` prefix coverage、load balance、collision P90/P99/P99.9；
- `[完成-实测]` codebook tensor 保存/恢复和 registry 生成。

### 4.2 数据层

- `[完成-本地]` 统一 `view/addtocart/transaction` schema；
- `[完成-本地]` RetailRocket `events.csv` 流式适配；
- `[完成-本地]` 严格早于 target 的 history；
- `[完成-本地]` 同毫秒事件互相不可见；
- `[完成-本地]` validation/test 可以使用更早 split 的历史；
- `[完成-本地]` 冷启动 target 与普通 benchmark 分离；
- `[完成-本地]` 确定性 reservoir 样本上限；
- `[代理-实测]` 两个 item-properties 文件的训练期属性代理向量。

### 4.3 模型、训练和评测

- `[完成-实测]` 小型 dense Transformer encoder-decoder；
- `[完成-实测]` padding、causal 和 cross-attention mask；
- `[完成-实测]` weighted NTP loss 和 backward；
- `[完成-实测]` 单 batch overfit；
- `[完成-实测]` checkpoint 恢复后 logits 对齐；
- `[完成-实测]` PrefixTrie greedy 和 beam search；
- `[完成-本地]` HR@K、Recall@K、MRR、NDCG、合法 SID 比例；
- `[完成-实测]` 10万样本、3 epoch 单卡 L20 训练。

## 5. 服务器验证记录

### 5.1 Toy 模型 CUDA

```text
loss=2.312395 -> 0.002499
generated=[[1, 2, 3], [7, 8, 9]]
```

验证了 forward、backward、overfit、checkpoint 和 constrained greedy。

### 5.2 Toy beam search CUDA

```text
beams=[[[1, 2, 3], [7, 8, 9]], [[7, 8, 9], [1, 2, 3]]]
```

验证了完整合法路径和 beam 排序控制流。

### 5.3 真实数据 frequency-bootstrap 训练

- 100,000 训练样本；
- 3 个 epoch 正常完成；
- train loss 正常下降；
- 具体数值因服务器信息安全要求未导出。

### 5.4 合成数据 PyTorch RQ

```text
shape=(3, 8, 16)
mse=0.927709 -> 0.006996
collision_rate=0.884766
```

高碰撞来自 width=8 的强聚类 fixture；该实验只验证实现正确性。

### 5.5 RetailRocket 属性代理 RQ：width=64

```text
represented_items=4808
mse=0.003906 -> 0.002105
collision_rate=0.911606
```

结论：模型/RQ 接口通过，但 registry 碰撞率不可接受。

### 5.6 RetailRocket 属性代理 RQ：width=256

```text
represented_items=18733
unique_vector_rate=1.000000
mse=0.003906 -> 0.001671
collision_rate=0.211071
```

结论：输入向量没有完全重复；扩大 codebook 明显降低碰撞，剩余问题主要在 RQ 容量和优化。

### 5.7 width=512 + K-Means++

- 状态：`[完成-实测]`；
- `represented_items=18733`；
- `unique_vector_rate=1.000000`；
- `mse=0.003906 -> 0.001596`；
- `collision_rate=0.444243`；
- epoch 和 train loss 正常；
- 结论：MSE 小幅改善但碰撞恶化，K-Means++ 优化目标与 SID 唯一性不一致。

### 5.8 最终 RQ 2x2 控制实验

- width=256/K-Means++：`mse=0.001678`，`collision=0.203331`，`unique_sids=16449`，prefix=`[256, 7434, 16449]`；
- width=512/random：`mse=0.001584`，`collision=0.325148`，`unique_sids=15001`，prefix=`[512, 11890, 15001]`；
- 最终选择：width=256/K-Means++，seed=17；
- 选择原则：优先降低 item collision 和提高完整 SID 基数，接受极小 MSE 差异；
- 服务器路径：`data/processed/rq_comparison/w256_kmeanspp/`。

### 5.9 Instruction + Q2I + IGR 合成 CUDA 闭环

```text
joint_loss=2.914326 -> -0.162442
ntp=0.024518
q2i=-0.934801
igr_shape=(4, 2)
instruction_delta=0.206740
```

结论：

- 联合目标可正常反向传播并明显下降；
- Q2I 的负值来自负余弦相似度主项，不代表数值异常；
- IGR 返回每条样本两个有效长历史商品，padding 防护通过；
- 改变 instruction feature 会改变生成 logits；
- 该结果只证明方法控制流和优化闭环，尚不证明 RetailRocket 上有推荐收益。

### 5.10 RetailRocket IGR+Q2I smoke test

配置：冻结的 width=256/K-Means++ registry，5,000 训练样本，20 验证样本，
short=20，long=50，top-k=5，单卡 CUDA，1 epoch。

```text
train_loss=5.139547
ntp_loss=5.235295
q2i_loss=-0.478739
HR@1=0.0
HR@5=0.0
MRR=0.0
legal_sid_rate=1.0
```

结论：

- `5.235295 + 0.2 × (-0.478739) = 5.139547`，联合损失数值关系正确；
- 真实数据的 instruction、IGR、Q2I、NTP、backward、优化和约束解码已贯通；
- 合法 SID 比例为 1，PrefixTrie 约束保持有效；
- 5,000 样本、1 epoch、20 个验证样本下零命中不能用于判断方法收益；
- 下一步必须在相同 cohort/seed/registry 上比较四组消融，而非单独解读该指标。

### 5.11 四组 20K 消融与 loss identity 复核

四组均完成 2 epoch，合法 SID 比例均为 1。第 2 epoch 汇总：

| Variant | NTP loss | HR@1 | HR@5 | MRR |
|---|---:|---:|---:|---:|
| Base | 4.222540 | 0.04 | 0.06 | 0.045 |
| Instruction | 4.225291 | 0.04 | 0.08 | 0.060 |
| IGR | 4.239571 | 0.04 | 0.08 | 0.049 |
| IGR+Q2I | 4.239382 | 0.04 | 0.08 | 0.049 |

当前不能宣称增益：验证集只有 50 条，HR@5 的一个命中即 0.02，方差很大。
IGR+Q2I 日志的 Q2I 项实际带负号，前一次文字转录漏掉了该符号。512 样本复核
得到逐 batch `loss_identity_error=0`、epoch `identity_error=5.877e-08`：
`5.665108 + 0.2 × (-0.074755) = 5.650157`。因此联合 loss 符号、权重和
日志聚合均正确，四组结果解除 `[待审计]` 标记。

### 5.12 三随机种子消融结果

| Variant | HR@1 | HR@5 | MRR | NDCG | NTP loss |
|---|---:|---:|---:|---:|---:|
| Base | 0.0600±0.0180 | 0.0833±0.0058 | 0.0673±0.0148 | 0.0713±0.0125 | 4.2219±0.0125 |
| Instruction | 0.0633±0.0236 | 0.0933±0.0284 | 0.0731±0.0249 | 0.0780±0.0258 | 4.2251±0.0128 |
| IGR | 0.0450±0.0260 | 0.0733±0.0362 | 0.0576±0.0297 | 0.0616±0.0313 | 4.2443±0.0067 |
| IGR+Q2I | 0.0417±0.0289 | 0.0767±0.0379 | 0.0559±0.0322 | 0.0612±0.0335 | 4.2448±0.0072 |

解释：Instruction 的均值小幅高于 Base，但标准差覆盖差值，不能宣称稳定提升；
IGR 两组均未带来收益。当前 `igr` 的 adapter 没有 Q2I 监督，而离散 top-k
阻断 NTP 到检索分数的梯度，因此该分支接近未训练检索器。后续增加 Q2I-only
对照和 SID-level repeat retrieval recall，先验证检索器是否真正学会取回相关历史。

## 6. 当前测试状态

本地 macOS Python 3.14 环境：

```text
Ran 49 tests
OK (skipped=21)
```

- 28 项不依赖本地 PyTorch/NumPy的测试通过；
- 21 项 PyTorch/NumPy测试因本机依赖缺失跳过；
- 跳过不等于通过，对应 CUDA 路径以服务器验证记录为准。

## 7. 当前关键风险

1. 商品向量仍是公开数据属性 hashing 代理，不是论文多模态编码器；
2. width=256 registry 的 colliding-item rate 仍为 21.11%；
3. 不应长期停留在 K-Means 调参；完成一次可用 registry 后必须进入 instruction/Q2I/IGR；
4. 当前模型是小型 dense 骨架，不是论文 0.7B/0.4B MoE 或最终规模；
5. beam search 是正确性 reference，不是高吞吐部署实现；
6. 当前只有单卡 L20 实测，没有 NCCL 多卡或 NPU 证据；
7. 公开数据指标只能和本项目 baseline/消融比较，不能横向声称复现论文私有主表。

## 8. 下一步优先级

### P0：冻结 Phase-1 SID

1. `[完成]` 完成 RQ 2x2 控制实验；
2. `[完成]` 冻结 width=256/K-Means++ 代理 registry；
3. `[完成]` 停止普通 K-Means 参数搜索；
4. `[后续消融]` 行为共现表示或更强多模态编码器；
5. `[当前主线]` 进入 instruction/Q2I/IGR。

### P1：进入 OxygenREC-v1 关键方法

1. `[完成-实测]` default/dense instruction、scenario 和 trigger 接口；
2. `[完成-实测]` 双 prompt-token instruction fusion；
3. `[完成-实测]` Q2I adapter、Eq.2 代理损失和联合 NTP；
4. `[完成-实测]` 冻结长历史分支、mask-safe cosine top-k IGR；
5. `[完成-实测]` 联合 loss、padding 检索保护和 instruction-logit 影响；
6. `[完成-本地]` 构造无重叠的 RetailRocket 长短历史 batch；
7. `[完成-待 CUDA]` Base、+Instruction、+IGR、+IGR+Q2I 共用训练/评测入口；
8. `[完成-实测]` 真实数据 `igr_q2i` 5,000 样本 smoke test；
9. `[完成-实测]` 四组 20,000 样本、2 epoch 中等规模单种子消融；
10. `[完成-实测]` batch/epoch loss identity 均通过，模型公式正确；
11. `[完成-实测]` 三个随机种子、200 验证样本的四组矩阵；
12. `[完成-实测]` Q2I-only 对照与 IGR SID repeat-recall 初步诊断；
13. `[完成-实测]` IGR repeat lift低于1且弱于recent baseline，检索质量门槛失败；
14. `[完成-失败]` Q2I初始化后IGR lift降至约0.817，排除随机初始化单因解释；
15. `[完成-改善]` trigger使repeat recall由约0.598升至0.720、lift由0.817升至0.984；
16. `[完成-失败]` 2,000 validation下lift=0.974，trigger仍稳定低于随机与recent；
17. `[完成-失败]` masked mean context的lift约0.971，未超过随机；
18. `[完成-波动]` attention epoch 1 lift约1.010，epoch 2回落至约0.984；
19. `[完成-失败]` attention epoch 1/2在2,000 validation上均低于随机；
20. `[已终止]` 不再继续行为SID query pooling调参；
21. `[完成-失败]` 属性side retrieval lift=0.992765，仍未超过随机；
22. `[路线冻结]` IGR控制流完成，但RetailRocket公开代理收益复现失败；
23. `[完成-实测]` SA-GCPO公式4-9全部核心分支CUDA通过；
24. `[完成-实测]` constrained beam、四类公开reward和policy update端到端CUDA闭环；
25. `[完成-代理实测]` teacher-augmented SA-GCPO使同cohort指标提升，target注入率为89.5%；
26. `[完成-代理实测]` 无target注入时同cohort指标仍提升，但存在训评同cohort泄漏；
27. `[完成-中性]` 独立train alignment cohort→held-out validation的泛化对照；
28. `[完成-证据不足]` 200条held-out上HR不变，MRR/NDCG只增加约0.001；
29. `[完成-单种子正向]` 1000条held-out的HR/MRR/NDCG均小幅上升，paired rank为39/21/940；
30. `[完成-已冻结]` 三种子paired rank净改善均为正，但HR cutoff方向不稳定；停止SA-GCPO调参。
31. `[完成-待CUDA]` Qwen Plan与长历史behavior已透传到greedy、beam search及rollout candidate log-prob。
32. `[完成-实测]` 服务器输出符合预期，`planned_paths=3`，v1最终生成透传缺口关闭。

### P1.5：Qwen LoRA SFT

1. `[完成-本地]` 定义system/user/assistant三段messages数据协议；
2. `[完成-本地]` 复用Reasoning schema和Plan编译器校验训练标签；
3. `[完成-本地]` 默认仅允许`review_status=approved`样本进入正式训练集；
4. `[完成-待服务器数据]` 将现有Qwen输出转为`pending`候选集；
5. `[未开始]` 扩充与审核SFT样本；
6. `[未开始]` Qwen3-4B LoRA最小训练与前后对照。

## 9. Phase 2 当前实现边界

- 论文中的 Scenario Instruction 被实现为 scenario embedding，并可叠加 trigger SID；
- Contextual Reasoning Instruction 支持外部 dense feature adapter，也支持缺失时的 learnable fallback；
- Q2I 使用当前公开代理 SID 的 item embedding，不等于论文的商品 ID、side-info、文本三路表示；
- IGR 对长历史 item vector 完整 detach，避免检索支路反向更新，随后将 top-k 商品拼到短历史；
- RetailRocket 没有搜索 query 和 Slow LLM 生成的 reasoning instruction，因此当前 CUDA 脚本只验证方法控制流，不能作为真实 instruction 效果结论。

### P2：行为与多场景

1. `[完成-待CUDA]` 将 view/cart/transaction 作为历史item token的行为条件；
2. 分行为训练和评测；
3. 明确 RetailRocket 中可用的公开场景代理，不能凭空创造工业场景事实。

### P3：LLM/Agentic Search交叉主线

1. 导出Contextual Reasoning Instruction的输入、结构化reason和最终instruction样例；
2. 导出IGR的query、候选、score、top-k及推荐结果链路；
3. 导出Q2I对齐前后的正/负样本和cosine变化；
4. 导出SA-GCPO的candidate、四类reward、advantage、threshold和ratio轨迹；
5. 再进入v2 trajectory/privileged feedback的GPU结构复现和样例review；
6. NPU、MoE、生产多场景和高性能推理全部暂缓。

## 10. 里程碑定义

| 里程碑 | 完成条件 | 当前状态 |
|---|---|---|
| M1：v1 核心可用 | 公开数据、SID、生成、解码、HR/Recall 闭环 | `[完成-代理]`，公开数据代理 SID 已冻结 |
| M2：v1 方法完整 | instruction、IGR、Q2I、至少一个偏好对齐机制 | `[完成-公开代理]` |
| M3：v2 预训练 | behavior instruction 相对 PT-only 有稳定收益 | `[完成-公开代理失败]` |
| M4：v2 后训练 | trajectory、privileged distillation、entropy 策略 | `[未开始]` |
| M5：双平台 | GPU/NPU 精度对齐和性能报告 | `[未开始]` |
| M6：规模化 | 3B-A1B MoE 稳定训练或推理 | `[未开始]` |

## 11. 更新规则

以后每次工作结束时更新以下内容：

1. 模块状态和对应文件；
2. 本地测试数量及跳过原因；
3. 服务器实测命令、硬件和可公开的聚合结果；
4. 论文事实、公开数据代理和工程假设的边界；
5. 当前阻塞和下一步 P0/P1；
6. 不用“脚本跑完”代替方法或指标验收。
7. 每次收到服务器实验输出后，先向 `复现实验日志.md` 追加带时间戳的实验记录，再更新本进度摘要。
