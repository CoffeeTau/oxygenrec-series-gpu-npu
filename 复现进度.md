# OxygenREC 复现进度

更新时间：2026-08-24  
当前阶段：Phase 1 - 最小 OxygenREC-v1 核心闭环，正在冻结公开数据代理 SID

## 1. 项目边界

本项目的准确表述是：

> OxygenREC 论文方法自实现 + 公开数据近似 benchmark + GPU/NPU 迁移与性能评测。

当前结果不能称为京东生产模型、私有数据主表或线上收益的严格复现。论文未公开完整工业数据、商品特征、训练代码、checkpoint、reward service 和线上协议。

状态标记：

- `[完成-实测]`：实现完成，并在目标服务器实际执行；
- `[完成-本地]`：实现完成，纯 Python 测试通过；
- `[代理-实测]`：公开数据替代方案已实际执行，但不是论文原始组件；
- `[待验证]`：代码已实现，但尚未在相应依赖或硬件上运行；
- `[未开始]`：尚未进入实现；
- `[阻塞]`：缺少数据、论文细节、硬件或外部依赖。

## 2. 总体进度

| 目标 | 当前判断 | 说明 |
|---|---:|---|
| 最小工程闭环 | 约 75% | 数据、SID、模型、解码、指标和 checkpoint 已贯通 |
| OxygenREC-v1 关键论文方法 | 约 30% | Fast backbone 骨架已完成；真实 instruction、Q2I、IGR、RL 未完成 |
| OxygenREC-v2 | 低于 5% | 只有接口设计预留，尚未实现行为条件预训练和后训练 |
| GPU/NPU 双平台 | 约 10% | 单卡 L20 基础验证完成；多卡 GPU、NPU 均未开始 |
| 3B-A1B MoE 最终目标 | 0% | 必须在 dense 方法闭环之后启动 |

百分比只用于项目监控，不代表论文指标复现比例，也不用于对外成果声明。

## 3. 论文方法对照表

| 论文模块 | 当前状态 | 当前实现 | 主要缺口 |
|---|---|---|---|
| 数据与时间协议 | `[完成-本地]` | RetailRocket 流式读取、全局时间切分、同毫秒隔离、train-only vocabulary | 尚未冻结正式数据版本和最终 cutoff |
| 多模态商品编码 | `[代理-实测]` | 训练截止前最新商品属性 + 256维 signed feature hashing | 未实现文本/图像编码、I2I 对比学习、Q-Former、MLP 融合 |
| 三层 RQ-KMeans | `[代理-实测]` | PyTorch 分块 Lloyd RQ，三层 codebook，width 可配置 | 论文只披露 3层×8192；初始化和训练细节未知，当前 width 较小 |
| SID registry/collision | `[完成-本地]` | 版本化 item↔SID、碰撞保留、JSON 持久化 | 尚未冻结可用于正式实验的低碰撞 registry |
| Fast encoder-decoder | `[完成-实测]` | 小型 dense Transformer encoder-decoder | 规模、层数和工业特征与论文不同 |
| level-aware SID embedding | `[完成-实测]` | 三层独立 embedding，历史商品层内求和 | 尚未进行论文规模消融 |
| 三层 NTP heads/loss | `[完成-实测]` | 三个 level-specific heads、加权交叉熵 | 论文完整权重和训练配方未披露 |
| Prefix 约束解码 | `[完成-实测]` | greedy + deterministic reference beam search | 尚未做高性能批量 beam/KV cache |
| HR/Recall/MRR/NDCG | `[完成-本地]` | SID→item、碰撞显式命中、合法 SID 比例 | 需在正式 registry 和固定 test split 上报告 |
| Contextual Reasoning Instruction | `[未开始]` | 只有 default instruction token 占位 | 缺 Slow LLM、模板、过滤、缓存和 instruction store |
| Instruction fusion | `[未开始]` | decoder 接口已预留 instruction ID | 尚无真实 instruction 表示和融合实验 |
| Q2I alignment loss | `[未开始]` | 无 | 需要 query/item adapter、对齐目标和 loss 权重 |
| Instruction-Guided Retrieval | `[未开始]` | 无 | 需要长历史检索、top-k、mask 和消融 |
| 多场景 instruction | `[未开始]` | 无 | RetailRocket 场景映射方案尚未定义 |
| Reward Mapping Service | `[未开始]` | 无 | 私有工业服务不可得，需要公开 benchmark 替代协议 |
| SA-GCPO | `[未开始]` | 无 | rollout、reward、reference/policy、温度策略均未实现 |
| v2 behavior instruction | `[未开始]` | 数据层已有 view/cart/transaction 枚举 | 未进入 behavior-prefix 和行为加权预训练 |
| v2 privileged distillation | `[未开始]` | 无 | 需要未来行为 teacher 和 entropy-aware 策略 |
| MoE | `[未开始]` | 无 | dense 闭环和分布式验证前不启动 |

## 4. 已完成基础设施

### 4.1 SID 与量化

- `[完成-本地]` `SemanticID`：三层 code 范围和层数校验；
- `[完成-本地]` `SIDRegistry`：版本化映射、碰撞保留、保存/加载；
- `[完成-本地]` `PrefixTrie`：合法前缀和 next-code 查询；
- `[完成-本地]` 纯 Python reference K-Means++、Lloyd K-Means、Residual K-Means；
- `[完成-实测]` PyTorch/CUDA 分块 Residual K-Means；
- `[完成-本地]` prefix coverage、load balance、collision P90/P99/P99.9；
- `[完成-实测]` codebook tensor 保存/恢复和 registry 生成。

### 4.2 数据层

- `[完成-本地]` 统一 `view/addtocart/transaction` schema；
- `[完成-本地]` RetailRocket `events.csv` 流式适配；
- `[完成-本地]` 严格早于 target 的 history；
- `[完成-本地]` 同毫秒事件互相不可见；
- `[完成-本地]` validation/test 可以使用更早 split 的历史；
- `[完成-本地]` 冷启动 target 与普通 benchmark 分离；
- `[完成-本地]` 确定性 reservoir 样本上限；
- `[代理-实测]` 两个 item-properties 文件的训练期属性代理向量。

### 4.3 模型、训练和评测

- `[完成-实测]` 小型 dense Transformer encoder-decoder；
- `[完成-实测]` padding、causal 和 cross-attention mask；
- `[完成-实测]` weighted NTP loss 和 backward；
- `[完成-实测]` 单 batch overfit；
- `[完成-实测]` checkpoint 恢复后 logits 对齐；
- `[完成-实测]` PrefixTrie greedy 和 beam search；
- `[完成-本地]` HR@K、Recall@K、MRR、NDCG、合法 SID 比例；
- `[完成-实测]` 10万样本、3 epoch 单卡 L20 训练。

## 5. 服务器验证记录

### 5.1 Toy 模型 CUDA

```text
loss=2.312395 -> 0.002499
generated=[[1, 2, 3], [7, 8, 9]]
```

验证了 forward、backward、overfit、checkpoint 和 constrained greedy。

### 5.2 Toy beam search CUDA

```text
beams=[[[1, 2, 3], [7, 8, 9]], [[7, 8, 9], [1, 2, 3]]]
```

验证了完整合法路径和 beam 排序控制流。

### 5.3 真实数据 frequency-bootstrap 训练

- 100,000 训练样本；
- 3 个 epoch 正常完成；
- train loss 正常下降；
- 具体数值因服务器信息安全要求未导出。

### 5.4 合成数据 PyTorch RQ

```text
shape=(3, 8, 16)
mse=0.927709 -> 0.006996
collision_rate=0.884766
```

高碰撞来自 width=8 的强聚类 fixture；该实验只验证实现正确性。

### 5.5 RetailRocket 属性代理 RQ：width=64

```text
represented_items=4808
mse=0.003906 -> 0.002105
collision_rate=0.911606
```

结论：模型/RQ 接口通过，但 registry 碰撞率不可接受。

### 5.6 RetailRocket 属性代理 RQ：width=256

```text
represented_items=18733
unique_vector_rate=1.000000
mse=0.003906 -> 0.001671
collision_rate=0.211071
```

结论：输入向量没有完全重复；扩大 codebook 明显降低碰撞，剩余问题主要在 RQ 容量和优化。

### 5.7 width=512 + K-Means++

- 状态：`[完成-实测]`；
- `represented_items=18733`；
- `unique_vector_rate=1.000000`；
- `mse=0.003906 -> 0.001596`；
- `collision_rate=0.444243`；
- epoch 和 train loss 正常；
- 结论：MSE 小幅改善但碰撞恶化，K-Means++ 优化目标与 SID 唯一性不一致。

### 5.8 最终 RQ 2x2 控制实验

- 已有：width=256/random、width=512/K-Means++；
- 待测：width=256/K-Means++、width=512/random；
- 直接复用完全相同的 `item_embeddings.npy`，不重新处理数据；
- 不重复训练推荐模型，只比较 MSE、collision、unique SID 和 prefix occupancy；
- 测试入口：`bash run_server_test.sh`。

## 6. 当前测试状态

本地 macOS Python 3.14 环境：

```text
Ran 36 tests
OK (skipped=10)
```

- 26 项不依赖本地 PyTorch/NumPy的测试通过；
- 10 项 PyTorch/NumPy测试因本机依赖缺失跳过；
- 跳过不等于通过，对应 CUDA 路径以服务器验证记录为准。

## 7. 当前关键风险

1. 商品向量仍是公开数据属性 hashing 代理，不是论文多模态编码器；
2. width=256 registry 的 colliding-item rate 仍为 21.11%；
3. 不应长期停留在 K-Means 调参；完成一次可用 registry 后必须进入 instruction/Q2I/IGR；
4. 当前模型是小型 dense 骨架，不是论文 0.7B/0.4B MoE 或最终规模；
5. beam search 是正确性 reference，不是高吞吐部署实现；
6. 当前只有单卡 L20 实测，没有 NCCL 多卡或 NPU 证据；
7. 公开数据指标只能和本项目 baseline/消融比较，不能横向声称复现论文私有主表。

## 8. 下一步优先级

### P0：冻结 Phase-1 SID

1. 完成最后两个 RQ 2x2 控制单元；
2. 对比 collision、MSE、prefix coverage 和 load balance；
3. 从四组中冻结当前最佳 `sid_registry` 版本；
4. 不再继续普通 K-Means 参数搜索；
5. 将行为共现表示列为后续 tokenizer 消融，主线进入 instruction/Q2I/IGR。

### P1：进入 OxygenREC-v1 关键方法

1. 实现真实 instruction 数据结构和 default fallback；
2. 实现 instruction embedding/fusion；
3. 实现 Q2I adapter 和 alignment loss；
4. 实现 IGR top-k 长历史检索；
5. 分别保留 Base、+Instruction、+IGR、+IGR+Q2I 消融。

### P2：行为与多场景

1. 将 view/cart/transaction 映射为行为 instruction；
2. 分行为训练和评测；
3. 明确 RetailRocket 中可用的公开场景代理，不能凭空创造工业场景事实。

### P3：后训练与平台迁移

1. 实现公开 benchmark 可验证 reward；
2. 再进入 GRPO/SA-GCPO；
3. 固定 GPU reference logits/loss/指标；
4. 单卡 NPU 对齐；
5. 多卡 GPU/NPU 和最终 MoE。

## 9. 里程碑定义

| 里程碑 | 完成条件 | 当前状态 |
|---|---|---|
| M1：v1 核心可用 | 公开数据、SID、生成、解码、HR/Recall 闭环 | `[进行中]`，工程闭环通过，SID 待冻结 |
| M2：v1 方法完整 | instruction、IGR、Q2I、至少一个偏好对齐机制 | `[未开始]` |
| M3：v2 预训练 | behavior instruction 相对 PT-only 有稳定收益 | `[未开始]` |
| M4：v2 后训练 | trajectory、privileged distillation、entropy 策略 | `[未开始]` |
| M5：双平台 | GPU/NPU 精度对齐和性能报告 | `[未开始]` |
| M6：规模化 | 3B-A1B MoE 稳定训练或推理 | `[未开始]` |

## 10. 更新规则

以后每次工作结束时更新以下内容：

1. 模块状态和对应文件；
2. 本地测试数量及跳过原因；
3. 服务器实测命令、硬件和可公开的聚合结果；
4. 论文事实、公开数据代理和工程假设的边界；
5. 当前阻塞和下一步 P0/P1；
6. 不用“脚本跑完”代替方法或指标验收。
