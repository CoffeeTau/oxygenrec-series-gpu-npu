结论：OxygenREC-v2 不是推翻当前 v1 重新搭模型，而是在 v1 的 SID + Encoder-Decoder GR + Contextual
  Reasoning 基础上，重点增加“行为条件生成”和一套新的后训练方法。

  可以先把两代模型理解成：

  当前 v1：
  用户历史 X → Encoder
  场景指令 Is + 推理指令 Ir → Decoder → 生成目标 SID

  OxygenREC-v2：
  用户历史 X → Encoder
  场景指令 Is + 推理指令 Ir + 行为指令 Ib
                                  ↓
                           Decoder → 生成多个商品 SID

  后训练阶段：
  同一个模型 + 未来目标前缀 F → 特权教师
                ↓
  通过熵门控和自蒸馏训练普通模型

  ## 一、当前代码中可以直接复用的部分

  以下 v1 能力仍然是 v2 的基础：

  - SID / RQ-VAE 语义编码
  - SID 注册表、合法前缀树、约束 Beam Search
  - Encoder-Decoder 生成式推荐骨架
  - 短期与长期行为序列编码
  - 场景指令 Is
  - Contextual Reasoning Instruction Ir
  - IGR 检索结果
  - Q2I 语义对齐
  - NTP/交叉熵训练
  - Qwen 生成结构化 reasoning 的能力

  所以前面完成的工作没有浪费。尤其是 Encoder-Decoder GR、Contextual Reasoning、IGR、Q2I，仍然处于
  v2 主干路径上。

  ## 二、v2 需要新增的核心部分

  ### 1. Behavior Instruction：行为指令 Ib

  v2 最直接的变化，是把期望行为作为 Decoder 输入的一部分：

  Ib = MLP(BehaviorEmbedding(behavior))

  行为大致包括：

  click / cart / order

  训练时，行为来自日志中目标商品对应的真实行为；推理时，由业务目标指定，例如：

  希望推荐容易购买的商品 → order instruction
  希望推荐可能点击的商品 → click instruction

  需要注意：当前代码里的 behavior_embeddings 是加在 Encoder 历史事件上的，属于“历史行为特征”；v2 的
  Ib 是加进 Decoder prefix 的“目标行为指令”。两者作用不同，当前实现还不等于论文里的 Behavior
  Instruction。

  ———

  ### 2. Behavior-aware Pretraining：行为感知预训练

  这里有三个变化。

  第一，样本不再只取一个 next-item。论文会从同一上下文中收集多个有区分度的目标行为，并展开或组合为
  训练数据。

  第二，输出从单个 SID：

  [B, 3]

  变成多个商品 SID 组成的列表：

  [B, N, 3]

  训练时通常展平为：

  [B, 3N]

  也就是一次生成多个候选商品。这是对当前单目标训练流程比较明显的一次改造。

  第三，不同行为使用不同 token 权重。论文默认示例为：

  click = 1.2
  cart  = 1.5
  order = 2.0

  一个商品的三个 SID token 继承同一个行为权重。

  当前项目的 sample_weights 是对整个样本加权；v2 需要做到列表内部“每个目标商品、每个 SID token”分别
  加权。

  ———

  ### 3. EA-TOSD：新的后训练方法

  这是 v2 最大的新模块，全称可以理解为“熵感知的轨迹优化自蒸馏”。

  主要包含四部分：

  - 对同一个输入采样多条生成轨迹。
  - 使用可验证的 SID token 命中奖励，不依赖额外 Reward Model。
  - 给教师模型加入训练期才能看到的未来目标前缀 F。
  - 根据教师输出熵，选择自蒸馏或正向 KL 对齐。

  其中“教师”和“学生”不是两个完全不同的模型，而是共享同一套模型参数：

  学生输入：[BOS, Is, Ir, Ib]

  教师输入：[BOS, Is, Ir, Ib, F]

  F 可以包含接下来一两个真实目标商品的 SID。它只在训练阶段出现，部署推理时不需要。

  低熵位置说明教师比较确定，使用基于优势值的自蒸馏；高熵位置说明教师仍不确定，则使用正向 KL，让学生
  学习教师的完整概率分布。

  当前项目里的 SA-GCPO、候选轨迹、log probability 和 reward 代码可以复用一部分基础设施，但当前 SA-
  GCPO 不是论文中的 EA-TOSD，需要新增：

  - 几何加权 SID token-hit reward
  - 最优轨迹选择
  - 未来目标前缀 F
  - 特权教师前向
  - token entropy 计算
  - 低熵自蒸馏损失
  - 高熵 Forward-KL
  - SFT/NTP 锚定损失

  ## 三、对当前代码的影响

   当前模块                   v2 处理方式
  ━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   SID / RQ-VAE               基本复用
  ─────────────────────────  ────────────────────────────────────────
   Encoder                    基本复用
  ─────────────────────────  ────────────────────────────────────────
   Decoder                    复用主体，修改输入 prefix 和多商品输出
  ─────────────────────────  ────────────────────────────────────────
   Contextual Reasoning Ir    继续保留
  ─────────────────────────  ────────────────────────────────────────
   IGR                        继续作为检索和上下文证据
  ─────────────────────────  ────────────────────────────────────────
   Q2I                        继续作为语义对齐方法
  ─────────────────────────  ────────────────────────────────────────
   历史 behavior embedding    可以保留，但不能替代 Ib
  ─────────────────────────  ────────────────────────────────────────
   单目标样本构造             需要扩展为行为标注和列表目标
  ─────────────────────────  ────────────────────────────────────────
   整样本权重                 改为目标级/token 级行为权重
  ─────────────────────────  ────────────────────────────────────────
   SA-GCPO                    仅复用采样、奖励等基础设施
  ─────────────────────────  ────────────────────────────────────────
   EA-TOSD                    需要新增
  ─────────────────────────  ────────────────────────────────────────
   MoE Decoder                可以最后再做，不是初步复现的前置条件

  ## 四、建议的 v2 复现顺序

  为了避免一次改动过大，我建议按下面顺序继续：

  1. 在当前单商品模型上加入真正的 Decoder Behavior Instruction Ib。
  2. 做受控测试：改变 click/cart/order 指令，确认生成 logits 和候选 SID 会变化。
  3. 加入行为目标展开和 token 级行为加权。
  4. 把单商品 SID 扩展成列表式多商品 SID 生成。
  5. 实现可验证的轨迹奖励和最优轨迹训练。
  6. 加入未来目标前缀 F 和特权教师。
  7. 实现熵门控、自蒸馏和 Forward-KL。
  8. 最后再考虑 MoE、模型放大和完整工程优化。

  因此，v2 可以概括成：

  v2 = v1 生成骨架
     + Decoder 端行为指令
     + 行为感知的列表式预训练
     + 特权未来信息教师
     + 熵门控轨迹自蒸馏

  真正工作量大的不是增加一个 Behavior Embedding，而是“列表式生成训练协议”和“EA-TOSD 后训练”。推荐特
  有的 SID/RQ-VAE 不需要继续投入大量时间；我们的重点可以放在行为条件指令、轨迹采样、可验证奖励、特
  权信息训练和自蒸馏，这些也更容易迁移到后续 Agentic Search 项目。

  论文依据：:codex-file-citation{path="/Users/coffeetau/projects/HuaWei/oxygenrec-series-gpu-npu/
  OxygenREC-v2.pdf" purpose="source"}