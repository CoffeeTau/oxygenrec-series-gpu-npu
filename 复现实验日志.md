# OxygenREC 复现实验日志

> 用途：按时间顺序记录每次模型实验的配置、输出、发现、判断、处理和后续动作。
> 本文记录实验推理过程；总体完成度与模块状态见 `复现进度.md`。

最后更新：2026-09-04 21:22:08 CST

## 记录规则

1. 新实验插入记录区最前，不覆盖旧结论；判断发生变化时，新增“纠正”记录并指向原记录；
2. 时间统一使用服务器或本地可确认的 `YYYY-MM-DD HH:MM:SS TZ`；
3. 历史对话未保留时分时写“具体时间未留存”，不补造精确时间；
4. 每条至少包含：实验目的、关键配置、输出、发现、判断、处理、结论和下一步；
5. 明确区分论文事实、公开数据代理、工程假设和目标服务器实测；
6. 因公司信息安全只保留允许公开的聚合信息，不记录服务器敏感路径、账号或环境秘密。

## 实验记录（最新在前）

### [2026-09-04 21:22:08 CST] E089：v1最终Plan生成链CUDA通过，进入SFT数据协议

- 服务器结果：用户确认本轮输出符合预期；greedy、beam和candidate log-prob三条路径均消费Retrieval Plan。
- 判断：E088的`待CUDA`状态转为`[完成-实测]`，OxygenREC-v1最终生成与rollout的Plan透传缺口正式关闭。
- SFT协议：新增`src/oxygenrec/sft_data.py`，将Reasoning review记录转为system/user/assistant三段messages；assistant标签复用严格JSON schema校验，Retrieval Plan必须能被IGR执行器编译。
- 质量门：正式训练默认只接受`review_status=approved`；`--allow-pending`仅用于生成待审核候选集，不允许宣称为train-ready。
- 工具：新增`scripts/prepare_qwen_sft_data.py`和`tests/test_sft_data.py`。
- 本地验证：SFT协议定向11项通过（其2项PyTorch检索执行测试跳过）；全量69项中43项通过、26项因本机缺PyTorch/NumPy跳过。
- 服务器入口：`bash run_server_test.sh`将现有三例输出转为`data/sft/qwen_reasoning_candidates.jsonl`待审核候选集。
- 结论：`[v1最终生成闭环完成-SFT数据协议已建立]`。

### [2026-09-04 21:14:51 CST] E088：Qwen Plan透传至最终生成与rollout概率路径

- 用户决策：暂停代码审阅，继续推进GPU复现。
- 问题：Qwen Retrieval Plan仅进入`OxygenRECModel.forward()`的IGR诊断路径，`generate()`、`beam_search()`和`candidate_log_probs()`不接收Plan，导致最终推理与训练/rollout语义不一致。
- 修复：三条路径新增`long_history_behavior_ids`和`retrieval_plans`参数，并透传到`_augment_history()`；group rollout中按候选数复制每条样本的Plan。
- 验证脚本：新增`scripts/validate_plan_generation_path.py`，同时验证greedy、beam、candidate log-prob均消费Plan，输出形状、有限分数和合法SID。
- 本地验证：Python AST和无PyTorch依赖测试通过；涉及PyTorch的测试因本机未安装PyTorch而跳过，不视为CUDA验收。
- 服务器入口：`CUDA_VISIBLE_DEVICES=0 bash run_server_test.sh`。
- 结论：`[生成路径代码透传完成-待CUDA实测]`。
- 下一步：CUDA通过后进入Qwen LoRA SFT的数据协议与最小训练闭环。

### [2026-08-28 11:16:53 CST] E087：暂停SFT，进入GPU v1代码理解与人工审阅

- 用户目标：先确认Contextual Reasoning、IGR、Q2I与Encoder-Decoder GR的真实完成边界，阅读并审阅代码，再决定后续SFT/RL。
- 完成判断：Encoder-Decoder GR、基础IGR、Q2I结构与训练、Qwen结构化Reasoning均已完成GPU方法验证；Qwen Plan目前只进入`forward()`的IGR/Encoder诊断路径，尚未透传到最终`generate()/beam_search()`。
- 注释范围：主链`events/temporal/model_inputs/SID/model/instructions/llm_features/llm_reasoning/retrieval_planning/evaluation`及RetailRocket训练入口的函数说明和关键张量语句已补充中文注释。
- 新指南：新增`代码阅读与数据流指南.md`，按事件→时间样本→SID→长短历史→instruction/query→IGR→Encoder→Decoder→NTP/Q2I→beam→指标解释数据流、张量形状和阅读顺序。
- README：更新已实现模块、审阅入口和未来工作，移除已经过时的“instruction/Q2I/IGR尚待实现”表述。
- 验证：核心Python语法通过；测试共65项，其中40项在本机实际通过，25项因本机缺PyTorch/NumPy按条件跳过；`git diff --check`通过。
- 代码边界：本轮只修改文档字符串、注释、README和指南，没有改变模型计算；工作区原有`项目重命名后续开发交接.md`删除状态未处理。
- 结论：`[SFT暂停-代码审阅材料完成]`。

### [2026-08-28 10:38:49 CST] E086：等权priority replay通过，Qwen Plan→真实IGR链收口

- 输出：`cases=8 changed=8 target_hits=0->1 high_intent_selected=3->3`，结果保存为`qwen_plan_retailrocket_equal_priority.jsonl`。
- 修复验证：将`priority_behaviors`从无依据的顺序降权改为集合等权后，目标SID正向命中仍保持`0->1`，而高意图候选从旧实现的`3->0`恢复为`3->3`。
- 根因结论：E083中高意图候选全部丢失主要由执行器错误地让第一项view权重大于第二项addtocart造成；不是模型forward、padding、Qwen JSON或Plan编译失败。
- 机制验收：8/8真实长历史top-k发生变化；Reasoning→白名单Plan→有界控制→模型IGR→SID gather→encoder链已在GPU实测贯通。
- 效果边界：8例只产生1个target gain、baseline命中为0，样本不足以证明统计提升；不能复现或声称论文工业收益。
- 停止决策：本轮已满足“链路work、无泄漏、合法候选、可导出正负例”的项目目标；不再围绕8例调整偏置或追逐RetailRocket代理指标。
- 后续方向：冻结当前等权执行语义，保留旧/新JSONL作为审计对照；转入真实Qwen的SFT/RL接口或trajectory optimization，不进入NPU。
- 结论：`[OxygenREC-v1真实Qwen Plan到IGR GPU方法链完成-代理效果不作论文收益结论]`。

### [2026-08-27 22:33:48 CST] E085：修正priority数组的无依据顺序权重并配置replay

- 规范纠正：当前schema只定义`priority_behaviors`为优先行为数组，没有声明先后次序代表权重等级；旧执行器按数组位置给予`0.15/(rank+1)`属于未记录的工程假设。
- 影响：Qwen四个双行为plan均输出`[view, addtocart]`，旧实现因此给view加`0.15`、addtocart只加`0.075`，会系统性压低高意图候选，可能直接造成E083的`3->0`。
- 修复：所有列入priority集合且真实出现的行为统一加`0.15`；未来只有schema显式输出数值权重时才允许非等权。
- Replay：真实评测脚本新增`--reasoning-input`，复用原8例Qwen reasoning并按相同seed重建validation cohort，只重跑checkpoint baseline/plan，不重新加载Qwen。
- 对照输出：结果另存为`qwen_plan_retailrocket_equal_priority.jsonl`，保留旧JSONL，确保前后版本可审计且不会覆盖原实验。
- 回归：新增“view/addtocart同基础分时调整后分数相同”的Torch测试；本地纯Python测试通过，Torch用例因本机无PyTorch跳过，语法与diff检查通过。
- 结论：`[执行语义错误已纠正-待同计划同样例CUDA replay]`。

### [2026-08-27 22:33:48 CST] E084：8例Plan分布与gain/loss归因

- Plan分布：priority为`[view,addtocart]`4例、`[view]`4例；8/8均`prefer_repeated_items=True`；recency/diversity组合为`balanced/true/medium`3例、`long_term/true/medium`4例、`long_term/true/high`1例。
- 模型行为：Qwen没有任何plan优先transaction，且repeat字段完全无变化，说明当前8例输出的控制多样性有限，存在计划塌缩迹象。
- 目标变化：仅case 7从`0->1`，无target loss；该例plan为`[view,addtocart] + balanced + repeat + medium`。
- 高意图变化：仅case 3减少2个、case 7减少1个；二者都是`[view,addtocart] + balanced + repeat + medium`，其余6例变化为0。
- 判断：目标增益与高意图下降同时发生在case 7，说明“高意图候选数量”不是目标相关性的充分代理；但case 3无目标增益却丢失2个高意图候选，暴露执行权重风险。
- 结论：`[归因完成-优先检查行为数组权重语义，不扩大样本]`。

### [2026-08-27 22:19:52 CST] E083：真实RetailRocket 8例paired首跑结果混合

- 输出：`cases=8 changed=8 target_hits=0->1 high_intent_selected=3->0`，真实Qwen生成、plan编译、checkpoint IGR和JSONL导出均完成。
- 机制结论：8/8 top-k发生变化，确认Plan控制在真实长历史而非合成候选上生效；模型接口与paired协议通过。
- 正向信号：目标SID检索命中由0增加到1，至少存在一个Plan纠正原semantic IGR候选的样例。
- 风险信号：被选中的addtocart/transaction事件总数由3降至0；Plan可能普遍偏向view、控制偏置过强，或原高意图事件与当前目标并不相关，当前汇总无法区分。
- 统计边界：8例、baseline零目标命中，方差极大；不能据此声称Plan提升，也不能因为高意图计数下降直接声称效果退化。
- 决策：暂不扩大样本、不调偏置权重；先对现有JSONL做plan分布与paired gain/loss归因，避免重复加载Qwen和盲目调参。
- 新工具：增加只读汇总脚本，输出priority/control组合计数、目标gain/loss case、高意图变化case和每例精简计划；不重新运行模型。
- 结论：`[真实链路通过-效果混合-先做归因诊断]`。

### [2026-08-27 22:10:38 CST] E082：真实RetailRocket Qwen Plan paired评测已配置

- 协议：从现有IGR-Q2I checkpoint保存的时间边界构造validation样例，固定seed抽8条；每条只使用target之前的真实历史统计生成Qwen plan。
- 成对控制：同一checkpoint、同一query、同一短历史和同一长历史候选分别运行原始semantic IGR与Plan IGR，避免模型和样例差异污染对比。
- 真实输入：Plan执行使用`LongShortSIDModelBatch`中的真实长历史SID、padding mask和behavior IDs，不再使用E080的人工候选池。
- 首轮指标：`changed`记录top-k候选列表变化数；`target_hits before->after`记录长历史中目标SID召回；`high_intent_selected before->after`记录被选addtocart/transaction事件数。
- 输出审计：逐例JSONL保留输入聚合证据、完整reasoning/plan、前后索引、目标SID命中和高意图候选计数；不保存用户明文或原始item ID。
- 资源控制：首轮仅8例；Qwen生成后释放generator并清理cache，再运行OxygenREC checkpoint，降低双模型并存显存。
- 边界：8例只用于端到端协议与方向检查，不能作为效果结论；通过后才扩大样本并增加paired rank/生成指标。
- 本地验证：Python/Shell语法、11项测试收集与diff检查通过；Torch相关5项因本机无PyTorch跳过。
- 结论：`[真实数据paired评测就绪-待L20首轮8例]`。

### [2026-08-27 21:58:03 CST] E081：Plan执行器已接入OxygenREC模型IGR路径

- 模型接口：`forward`新增`long_history_behavior_ids`与`retrieval_plans`可选输入，并透传到`_augment_history`；不提供plan时保持原cosine top-k路径不变。
- IGR集成：长历史item embedding与query继续产生原始semantic scores；提供plan时调用白名单执行器选出top-k，再按原逻辑gather SID并拼接短历史进入encoder。
- 输入校验：plan路径强制要求长历史behavior ID与score窗口形状一致，避免行为偏置错位；padding与候选数量继续沿用原校验。
- CUDA测试升级：同一脚本先验证纯执行器改变选择，再实例化完整OxygenREC encoder-decoder，通过公开`forward`接口检查`model_igr`索引形状与合法性。
- 兼容性：旧训练、评测和无plan checkpoint调用无需增加参数；本次只新增可选路径，不改变既有默认结果。
- 本地验证：共11项测试收集通过，其中6项纯Python通过、5项Torch测试因本机无PyTorch跳过；Python/Shell语法与diff检查通过。
- 结论：`[模型IGR集成完成-待L20端到端forward smoke]`。

### [2026-08-27 21:56:12 CST] E080：三个真实Qwen Plan受控检索执行CUDA通过

- 输出：`OK device=cuda cases=3 plan_changed=[True, True, True]`；三例baseline均为`[0,1,2]`，plan选择分别为`[0,4,3]`、`[0,1,4]`、`[0,4,3]`。
- 验证内容：服务器生成的三条真实Qwen plan均可通过事实护栏编译，并在CUDA tensor候选池上改变top-k选择；未出现非法行为、非有限分数或执行异常。
- 行为解释：high-intent与repeat-browse的行为/重复/中等多样性控制保留高分重复项，同时引入其他view/addtocart候选；browse-only的低多样性允许重复SID共存，并补充另一个view候选。
- 通过边界：候选SID、behavior与semantic score是人为构造的受控fixture，本结果证明的是“Reasoning→Plan→数值检索控制”机制，不代表RetailRocket HR/MRR提升，也不代表已经影响模型`_augment_history`真实路径。
- 判断：确定性执行器机制通过，不再增加合成调参；下一阶段将其接入模型长历史IGR，并在真实候选历史上记录baseline/plan的paired变化。
- 结论：`[Qwen Plan受控执行实测完成-进入真实IGR集成]`。

### [2026-08-27 21:41:34 CST] E079：Qwen Retrieval Plan确定性执行层已实现

- 输入边界：执行层只消费`priority_behaviors`、`recency`、`prefer_repeated_items`和`diversity`；生成的intent/evidence/strategy自由文本不进入检索分数。
- 事实护栏：priority behavior必须属于固定白名单且在输入`behavior_counts`中真实出现；未出现的行为会被过滤，全部未出现则拒绝执行。
- 执行映射：行为优先级、recent/balanced时间位置、重复SID偏好均转换为有界分数偏置；medium/high diversity在逐项top-k时惩罚或屏蔽完全相同SID。
- 主次关系：原始Q2I/IGR semantic score仍是主分数，LLM plan只提供小幅控制偏置，避免单次自然语言判断直接覆盖模型相关性。
- CUDA fixture：读取服务器已生成的`qwen_reasoning_cases.jsonl`，编译三个真实plan，在同一受控候选池上对比baseline与plan selection，并输出逐例`plan_changed`和索引。
- 本地验证：plan编译正反例与原Reasoning schema共7项通过；Torch执行测试因本机无PyTorch跳过，Python/Shell语法及diff检查通过。
- 服务器入口：`run_server_test.sh`已切换为计划执行测试，不重新加载约8GB Qwen权重。
- 结论：`[Reasoning到IGR控制层已接通-待CUDA受控选择验证]`。

### [2026-08-27 21:41:34 CST] E078：Retrieval Plan v2三例内容人工验收

- High-intent：38 view、2 addtocart、1 transaction、最近行为和重复商品数均来自输入；没有猜目标商品。`long_term + repeated + medium diversity`可执行，但把2次加购表述为“明确购买意图”、把10种重复商品写成检索上限均属于无依据强化；plan还未优先transaction。
- Browse-only：`view-only + long_term + repeated + low diversity`与统计方向一致，无目标泄漏；但“持续关注特定商品类目”无法由仅含行为类型和重复商品数量的输入推出。
- Repeat-browse：`view/addtocart + long_term + repeated + medium diversity`与长期重复浏览方向一致，无目标泄漏；但把22种重复商品说成多个“品类”、把少量加购解释为转化意愿强弱，超出了输入粒度。
- 共性判断：evidence已经从机械重复view改善为聚合事实；事实数字基本忠实，但事实与解释没有严格分层，存在商品/类目混淆和意图强度过度推断。
- 验收结论：无泄漏通过、plan schema与可执行性通过、证据严格忠实度部分通过。为避免继续陷入prompt微调，本阶段不追求语言完美，改由确定性执行护栏只消费白名单plan。
- 结论：`[内容质量部分通过-允许进入受控IGR执行验证]`。

### [2026-08-27 21:30:55 CST] E077：Retrieval Plan v2扩展预算后CUDA生成通过

- 输出：`OK device=cuda cases=3 schema_valid=3 peak_allocated_gib=7.805`，JSONL与Markdown均成功导出。
- 验证：Qwen3-4B本地权重加载、left-padding批量生成、384 new-token预算、完整JSON边界解析和v2严格schema在L20上贯通。
- 对E076的判断：增加预算和解析边界修复后相同三例全部通过，支持“v2嵌套JSON在256-token预算下被截断/错误截取”是上一轮失败的主要原因；由于未保存上一轮raw text，不能把截断认定为唯一原因。
- 显存：峰值约`7.805 GiB`，相比v1约`7.720 GiB`只增加约`0.085 GiB`，单卡L20余量充足。
- 通过边界：`schema_valid=3`只证明生成结构和字段类型合法，尚不能证明evidence忠实、无目标泄漏以及retrieval plan与证据一致。
- 下一步：人工检查三例Markdown中的Evidence、Retrieval plan与Constraints；通过后把白名单plan映射到IGR可执行控制参数，并做受控检索变化测试。
- 结论：`[Retrieval Plan v2 CUDA结构验收通过-等待内容质量review]`。

### [2026-08-27 17:30:37 CST] E076：Retrieval Plan v2首跑JSON解析失败及截断诊断修复

- 服务器报错：Qwen权重加载完成，但首批生成在`parse_reasoning_json`触发`JSONDecodeError: Expecting ',' delimiter`，位置约为第494字符；3例未能导出。
- 直接原因：模型输出以`{`开头但不是完整合法JSON；旧解析器用第一个`{`和最后一个`}`截取，可能把尚未闭合的外层对象中`retrieval_plan}`的右括号误当成完整JSON结尾。
- 首要推断：v2新增嵌套plan后仍只提供256 new tokens，错误位置和输出规模符合生成在外层对象闭合前触及长度上限；现有截图不足以证明唯一根因，因此保留格式遵循失败这一备选判断。
- 修复一：输出预算由256提高到384 new tokens，不改变模型权重、数据或解码随机性。
- 修复二：改用`JSONDecoder.raw_decode`识别第一个完整JSON对象；完整对象后的说明文字或额外花括号不再破坏解析，但不自动修补真正的残缺JSON。
- 修复三：解析失败新增`case index`、`hit_token_limit`、`generated_chars`诊断；若复测仍失败，可直接区分长度截断与模型语法失败。
- 回归：新增嵌套JSON截断拒绝和完整对象后杂文本容忍测试；本地4项单测、Python/Shell语法与diff检查通过。
- 结论：`[根因高概率为长度截断-诊断与预算已修-待L20复测]`。

### [2026-08-27 17:13:14 CST] E075：Reasoning输出升级为可执行Retrieval Plan

- Padding修复：decoder-only批量生成显式设置tokenizer `padding_side=left`，消除right-padding导致最后上下文位置错误的风险。
- Generation修复：greedy模式显式清空temperature/top-p/top-k，避免模型generation config中的采样参数被忽略警告。
- Schema升级：新增`retrieval_plan`，强制包含`priority_behaviors[]`、`recency`、`prefer_repeated_items`和`diversity`，并校验行为白名单、枚举和布尔类型。
- Evidence约束：必须是1-6条聚合事实，system prompt禁止逐条重复view，解决E074中browse样例证据冗长问题。
- 可执行性：上述plan字段可直接映射为IGR候选行为过滤/加权、时间衰减、重复商品boost和多样性约束；不再依赖解析自由文本strategy。
- 输出长度：提高到256 new tokens，为严格JSON schema保留空间。
- 本地验证：新版schema正反例、Python/Shell语法及diff检查通过；待L20重新生成。
- 结论：`[结构化检索计划v2就绪-待CUDA与人工review]`。

### [2026-08-27 17:13:14 CST] E074：Qwen结构化Reasoning三例CUDA生成通过但计划粒度不足

- 输出：3例全部`schema_valid`，peak allocated约`7.720 GiB`，JSONL/Markdown正常生成。
- High-intent：正确引用38 view、2 add-to-cart、1 transaction、最近混合行为和重复商品10；购买/高关注意图有证据，constraints明确不猜目标，质量较好。
- Browse-only：无目标泄漏，但evidence机械列出大量`view`，没有压缩为`view=41`和重复商品2；strategy“聚合重复访问商品种类”方向合理但仍是自由文本。
- Repeat-browse：识别长期/重复浏览方向，未猜具体商品；evidence同样重复view，且“高意图线索”只能由较早的2 cart/1 transaction弱支持，需更精确区分recent与long-term。
- 共性缺口：输入只有聚合统计、没有候选item/SID/时间列表，因此输出只能提供策略级计划，无法指定具体检索对象；当前`retrieval_strategy`也不能直接由程序稳定执行。
- 运行警告：decoder-only模型检测到right padding；虽然三例JSON合法，批量生成语义可能受padding位置影响，必须修复后再作为质量结论。
- 判断：真实Slow LLM生成、格式遵循和基础证据忠实度通过；可执行计划和证据压缩未通过。
- 结论：`[生成链通过-质量部分通过-需修padding与plan schema]`。

### [2026-08-27 17:00:58 CST] E073：真实Qwen结构化Reasoning生成与Review链已实现

- 目的：从“使用因果LM内部hidden state做代理embedding”转向论文Slow LLM更接近的角色，让Qwen实际生成可读、可审计、可执行的reasoning/instruction。
- Schema：强制JSON包含`intent`、`evidence[]`、`retrieval_strategy`和`constraints[]`；解析器允许外围误输出但严格验证字段类型和缺失项。
- 防泄漏prompt：system明确只能使用输入历史证据，不得猜测下一行为、目标商品或未提供属性；user只含历史长度、行为计数、最近行为和重复商品数。
- CUDA样例：high-intent、browse-only、repeat-browse三类证据；冻结Qwen3-4B、greedy deterministic generation、最多192 new tokens。
- 输出：`qwen_reasoning_cases.jsonl/.md`，每例保留输入证据、完整prompt、raw text、parsed JSON和三项人工review问题。
- 验收：三例均须schema valid；随后人工检查证据忠实度、目标泄漏和检索策略可执行性，而不是只看embedding cosine。
- 本地验证：JSON提取/类型/缺字段测试、Python/Shell语法和diff检查通过；真实生成待L20。
- 结论：`[Slow LLM公开替代链实现-待CUDA生成与人工review]`。

### [2026-08-27 17:00:58 CST] E072：Last-token池化优于Mean但区分度仍有限

- 输出：mean delta `0.049690`、mean cosine `0.998765`；last-token delta `0.105424`、last-token cosine `0.994443`；determinism error `0`，last驱动的logit delta `0.076775`，adapter grad `2.435029`，peak约`7.599 GiB`。
- 判断：last-token的欧氏区分约为mean的2.12倍，且下游logit影响更强，可作为hidden-state直接接入的基线。
- 边界：cosine仍高达0.994443；Qwen causal hidden state并非专门训练的sentence embedding，继续搜索pooling细节的学习收益有限。
- 决策：冻结last-token hidden-state基线，不再试更多池化；下一阶段让Qwen生成结构化reasoning文本，直接review Slow LLM输出质量和可执行检索意图。
- 结论：`[池化选择完成-last-token保留-转结构化生成]`。

### [2026-08-27 16:39:15 CST] E071：增加Qwen Mean与Last-token池化对照

- 动机：E070中mean-pooled特征余弦`0.998765`，两个行为prompt区分度偏弱；不能在未比较表示提取方式前批量缓存全数据。
- 实现：同一冻结Qwen、同一prompt和最后层hidden state同时支持`mean`与`last_token`；last位置由attention mask计算，兼容左右padding。
- 控制：mean重复编码继续检查确定性；adapter改用last-token特征验证logit变化和梯度；两种方式分别输出delta/cosine。
- 验收边界：不人为规定cosine阈值，先以相同样本实测比较；若last-token仍近乎重合，再进入结构化文本生成/专用embedding模型路线，而不是强行训练Q2I放大噪声。
- 服务器入口：模型已下载，无需重新下载；原样运行`CUDA_VISIBLE_DEVICES=0 bash run_server_test.sh`。
- 结论：`[池化对照就绪-待L20选择真实特征方案]`。

### [2026-08-27 16:39:15 CST] E070：Qwen3-4B真实Hidden-state接入CUDA通过

- 输出：`hidden_size=2560`，tokens `(108,105)`，feature delta `0.049690`，feature cosine `0.998765`，determinism error `0`，logit delta `0.028256`，adapter grad约`2.09509`，peak allocated约`7.590 GiB`。
- 通过项：官方模型本地加载、chat-template prompt、冻结forward、普通tensor边界、OxygenREC adapter前向/反向和显存记录全部通过。
- 显存判断：峰值约7.59 GiB，明显低于L20约44.40 GiB；冻结特征提取和后续adapter训练空间充足。
- 表示风险：两个prompt的cosine高达0.998765；整段mean pooling受到大比例共享模板token影响，虽然链路work，但不能直接认为具备足够的行为语义区分度。
- 决策：暂不批量缓存RetailRocket；先对比last-token pooling，再决定Qwen hidden state、生成后文本表示或专用embedding路线。
- 结论：`[真实Qwen GPU接口通过-表示区分度待改进]`。

### [2026-08-27 16:30:33 CST] E069：修复Qwen冻结特征与可训练Adapter的Tensor边界

- 服务器进展：Qwen权重3/3分片成功加载并完成冻结encoder forward，说明ModelScope下载、本地Transformers加载、BF16单卡显存和prompt编码已越过前置阻塞。
- 报错：OxygenREC `instruction_feature_adapter`前向时触发`RuntimeError: Inference tensors cannot be saved for backward`。
- 根因：Qwen特征在`torch.inference_mode()`内创建，属于PyTorch inference tensor；adapter权重需要梯度，Linear backward必须保存输入，因此不能直接接收该tensor。
- 修复：保持Qwen forward位于inference mode；退出该上下文后执行`features.clone()`，生成普通detached tensor，再进入可训练adapter。不会把梯度传回Qwen，也不增加Qwen activation graph。
- 回归测试：新增“inference tensor在上下文外clone后可驱动Linear weight backward”的测试；本机无PyTorch故跳过，服务器本轮将同时验证。
- 无需操作：不需要重新下载8GB模型、不需要修改CUDA/PyTorch、不需要启用Qwen梯度。
- 下一步：上传修复后原样重跑`CUDA_VISIBLE_DEVICES=0 bash run_server_test.sh`。
- 结论：`[根因已修复-待L20复测]`。

### [2026-08-27 12:41:06 CST] E068：按用户要求提供进程级临时关闭TLS验证

- 授权：用户明确要求先关闭SSL认证完成模型下载。
- 实现：下载脚本新增`--insecure-skip-tls-verify`；仅monkey-patch当前Python进程的Requests Session令`verify=False`，不修改系统CA、不设置全局环境变量。
- 审计：启动时打印`tls_verification=false`警告；下载完成后在`MODEL_SOURCE.json`永久记录`tls_verification: false`。
- 风险：该模式无法通过TLS保证ModelScope响应和8GB权重的来源真实性；只能作为公司受控代理环境下的临时实验通路。
- 恢复：不加该参数即自动恢复正常证书验证；后续取得公司CA后应使用可信链重新验证或重新下载。
- 结论：`[临时不安全下载通路已显式实现-作用域仅当前进程]`。

### [2026-08-27 12:38:54 CST] E067：ModelScope下载被公司代理CA阻断

- 报错：访问`www.modelscope.cn:443/api/v1/models/...`时出现`SSLCertVerificationError: self-signed certificate in certificate chain`，最终为Requests/urllib3 `MaxRetryError`。
- 诊断：DNS、代理路由和TCP/TLS握手已到达ModelScope；失败点是Python Requests不信任公司TLS检查代理签发链中的根/中间CA，不是模型ID、ModelScope SDK或下载目录错误。
- 推荐修复：由运维提供公司批准的PEM CA bundle，通过`REQUESTS_CA_BUNDLE`和`SSL_CERT_FILE`注入当前shell/任务；更完整方案是由运维加入容器/系统CA store。
- 验证顺序：确认Python的certifi路径；使用相同CA bundle对一个ModelScope只读URL做Requests smoke；成功后再重跑下载脚本。
- 安全边界：不在8GB模型权重下载中设置`verify=False`、清空CA校验或使用来源不明证书；否则无法可靠验证代理后的下载来源与内容。
- 备选：使用公司内部模型仓库/制品库；或在能验证官方TLS的机器下载完整snapshot和来源记录，再按公司流程传入服务器。
- 结论：`[外部依赖阻塞-需要公司CA或可信离线传输，不改模型代码]`。

### [2026-08-27 12:31:01 CST] E066：Qwen下载后端切换为ModelScope

- 原因：服务器代理无法连接Hugging Face；不继续在HF网络路径或关闭TLS验证上消耗时间。
- 官方核对：ModelScope存在Qwen官方`Qwen/Qwen3-4B-Instruct-2507`，SDK支持`snapshot_download(model_id, revision, local_dir, max_workers)`。
- 下载器：新增`--provider {modelscope,huggingface}`并默认ModelScope；默认revision为`master`，目标目录仍为`models/Qwen3-4B-Instruct-2507`，后续Transformers本地加载路径不变。
- 来源审计：`MODEL_SOURCE.json`记录provider、repo、revision、分片名称与字节数；HF备用后端额外解析并记录commit。ModelScope未从截图/SDK稳定取得commit时不伪造该字段。
- 依赖：服务器快照未发现ModelScope，指南增加`python -c`检查和`python -m pip install modelscope`；明确不升级现有PyTorch/CUDA/Transformers。
- 指南：主命令改为`python scripts/download_qwen_model.py --provider modelscope`，补充ModelScope续传、官方模型页和HF备用命令。
- 本地验证：CLI帮助、Python语法、Markdown章节编号及diff检查通过；未从本机实际下载权重。
- 结论：`[ModelScope下载路径就绪-待服务器安装SDK和下载]`。

### [2026-08-27 12:10:33 CST] E065：Qwen服务器下载与接入指南已交付

- 用户需求：模型直接下载到服务器，需要随项目上传、可边看边执行的中文说明，避免依赖当前对话和长命令手输。
- 指南：新增根目录`Qwen服务器接入操作指南.md`，覆盖项目定位、磁盘/GPU/版本检查、下载、断点续传、文件验证、单卡smoke、验收字段和最小安全日志。
- 下载工具：新增`download_qwen_model.py`，默认从官方`Qwen/Qwen3-4B-Instruct-2507`下载到`models/Qwen3-4B-Instruct-2507`；先解析官方commit再固定snapshot，并写`MODEL_SOURCE.json`保存repo、commit和权重分片。
- 安全：测试加载设置`local_files_only=True`和`trust_remote_code=False`；指南不建议关闭模型权重TLS验证，SSL失败时优先修复CA/使用公司批准代理或从可验证机器传输完整snapshot。
- 环境保护：明确不为单个错误随意升级现有NVIDIA定制PyTorch/CUDA；本阶段不要求DeepSpeed、bitsandbytes、FAISS或FlashAttention。
- 可操作性：默认只需`python scripts/download_qwen_model.py`和`CUDA_VISIBLE_DEVICES=0 bash run_server_test.sh`两条主命令；非默认目录可用`QWEN_MODEL_PATH`覆盖。
- 本地验证：下载脚本CLI、Python语法、Markdown结构和diff检查通过；未在本地实际下载8GB权重。
- 结论：`[服务器操作文档完成-等待用户下载与CUDA smoke]`。

### [2026-08-27 11:09:22 CST] E064：真实Qwen冻结特征接口已实现

- 选型：第一阶段使用官方`Qwen3-4B-Instruct-2507`；4B dense、BF16权重约8.06 GB、hidden size 2560，官方要求Transformers>=4.51，服务器4.57.6满足。
- 环境依据：单卡L20约44.40 GiB；当前无bitsandbytes/DeepSpeed，因此优先使用原生BF16冻结推理，不从4-bit QLoRA起步。
- 新接口：`FrozenLLMInstructionEncoder`只从本地目录加载AutoModel，禁用隐式下载和remote code，使用官方chat template，冻结全部参数，masked mean-pool最后层hidden state并L2归一化。
- Prompt：只包含严格早于target的历史长度、behavior counts、最近行为和重复访问数，并明确要求不得猜测下一行为/商品。
- CUDA fixture：两条不同真实行为prompt重复编码，检查hidden size、token counts、feature delta/cosine、确定性误差；随后将特征送入OxygenREC adapter，检查logit delta、adapter梯度和峰值显存。
- 服务器入口：`run_server_test.sh`默认读取`models/Qwen3-4B-Instruct-2507`，也支持`QWEN_MODEL_PATH`覆盖；本地模型缺失时明确失败。
- 文档：新增`docs/qwen_integration.md`记录型号依据、边界、目录、验收和SFT/RL后续顺序。
- 本地验证：prompt单测、Python/Shell语法和diff检查通过；本机无权重/PyTorch，真实数值待L20。
- 结论：`[真实LLM接口完成-待权重就位和CUDA smoke]`。

### [2026-08-27 10:55:45 CST] E063：GPU服务器环境截图已转为长期文本快照

- 来源：逐页视觉检查`GPU信息.pdf`的6页图片，原采集时间为`2026-08-21T16:51:40.986654+00:00`。
- 核心硬件：8×NVIDIA L20，compute capability 8.9，每卡`47677177856` bytes（约44.40 GiB，`nvidia-smi`为46068 MiB），92个SM；Driver `570.172.08`。
- 主机：Linux 5.15、x86_64、176逻辑CPU、约881.51 GiB内存；Python 3.12.3。
- 核心软件：NVIDIA定制PyTorch `2.11.0a0...nv26.3`、CUDA runtime 13.2、cuDNN 92000、NCCL 2.29.7、Transformers 4.57.6、Accelerate 1.14.0、FlashAttention 2.7.4系、Triton 3.6.0系。
- 缺失依赖：DeepSpeed、bitsandbytes、FAISS、xformers均未安装；不能直接假设ZeRO、4-bit QLoRA或向量索引可运行。
- 截图边界：GPU拓扑矩阵、NVCC完整版本和部分git revision未完整显示，文本档案明确标注为未确认。
- 输出：新增`docs/server_environment_snapshot_2026-08-21.md`并从环境采集说明页链接；原PDF删除后仍可据此进行Qwen/SFT/RL初步选型。
- 结论：`[服务器环境已文本化保留-原PDF可在用户确认后删除]`。

### [2026-08-27 10:47:58 CST] E062：Hash文本Checkpoint最终定性Review完成

- 汇总：CUDA成功导出5例；`igr_repeat_hits=0`，`beam_target_hits=0`；JSONL和Markdown均正常生成。
- Case 001：目标behavior为view，目标SID `[188,100,164]`；120条历史含117 view、2 add-to-cart、1 transaction，最近高意图事件为0、重复商品种类22。
- Reasoning审计：“近期以连续浏览为主，结合长期历史检索稳定兴趣商品”与最近历史证据一致；报告确认`proxy_text_used=True`、`slow_llm_text_used=False`，实际source为`hashed_proxy_text_plus_history_context_attention`。
- IGR：Top-10均为view，距target约197-413小时，全部与target SID匹配`0/3`层；多次重复取回SID `[5,54,54]`；score高度集中在约`0.9764-0.9791`，未取回目标SID。
- Q2I：单例alignment loss约`-0.963732`，cosine proxy约`0.963732`；再次说明数值对齐高不保证真实检索语义相关。
- Generation：5个beam均legal，collision为1或3，但target hit全部False；与全5例汇总零命中一致。
- 最终判断：文本实际输入、Q2I、IGR和constrained beam结构均work；hash instruction语义质量、检索区分度和端到端效果失败。该代理到此冻结，不再调模板、维度或训练轮数。
- 下一步：进入真实开源LLM表示/SFT接口设计；选择具体Qwen规格前需重新取得项目内未保存的服务器环境PDF或允许公开的显存/软件版本摘要。
- 结论：`[GPU结构复现与定性验收完成-Hash代理效果失败并冻结]`。

### [2026-08-27 10:24:25 CST] E061：实际文本输入Checkpoint最终样例Review已配置

- 目的：在冻结hash文本效果试验前，使用`igr_text_q2i/epoch-2.pt`导出最终5例定性轨迹，确认报告中的instruction就是模型实际收到的文本代理。
- 修正：review端复用训练时完全相同的`build_history_instruction`与64维编码，不再使用措辞不同的展示文本；实际scenario固定为0，与无泄漏训练一致。
- 报告字段：同时显示`proxy_text_used=True`、`slow_llm_text_used=False`和`hashed_proxy_text_plus_history_context_attention`，避免把hash代理误称为Slow LLM。
- 输出：`llm_cross_cases_text_instruction.jsonl/.md`；继续包含行为证据、IGR候选、Q2I、beam合法性与命中。
- 结论：`[最终定性review待CUDA导出]`。

### [2026-08-27 10:24:25 CST] E060：严格消融判定Hash文本代理无稳定收益

- Generic epoch 2：train loss `4.013643`，NTP `4.171968`，Q2I `-0.791627`，HR@1/5/10 `0.074/0.112/0.160`，MRR `0.094784`，Q2I alignment `-0.970751`。
- Generic IGR：repeat recall约`0.67769`，random约`0.68577`，lift约`0.98821`，repeat eligible `121`。
- 与Text epoch 2比较：HR@1相同；Text HR@5高`0.002`，但HR@10低`0.018`、MRR低约`0.00432`；Text repeat recall低约`0.03306`、lift低约`0.04821`；Q2I alignment几乎相同。
- 判断：三分类历史hash文本没有带来一致的生成或检索增益，且检索指标更差；不能将E057的训练内增长解释为文本收益。
- 决策：机制复现通过、公开hash代理效果失败并冻结，不继续调整模板/hash维度；完成实际输入样例review后转向真实开源LLM表示接口。
- 结论：`[严格消融完成-代理效果失败-停止调参]`。

### [2026-08-27 10:06:59 CST] E059：冻结未来开源基座模型的生产定位

- 决策：后续生产形态可引入适配服务器的Qwen类开源基座做Slow LLM instruction/reasoning，并采用LoRA/QLoRA SFT；在稳定SFT之后再以轨迹/排序reward做偏好优化或RL。
- 模块边界：Q2I是文本/query与商品表示的对齐目标和projection层，IGR是向量检索机制；二者不会被Qwen直接替换。
- 推荐架构：Slow LLM生成结构化instruction，Q2I形成检索query，IGR/ANN取候选，Fast模型完成受约束生成；必要时再由LLM rerank，最终蒸馏/量化部署。
- 当前hash encoder定位：仅用于验证接口、因果控制和公开数据链；未来可替换为Qwen hidden state而不重写Q2I/IGR接口。
- 结论：`[生产路线明确-当前仍先完成GPU公开代理验收]`。

### [2026-08-27 10:06:59 CST] E058：无文本通用Scenario严格基线已配置

- 目的：隔离E057的收益究竟来自历史文本instruction，还是随机种子、cohort、target-behavior scenario或其他配置差异。
- 新变体：`igr_generic_q2i`与`igr_text_q2i`共享registry、seed、样本数、长短历史、attention context、Q2I、IGR和训练超参；两者均固定generic scenario。
- 唯一主要差异：基线使用可学习常量instruction embedding；实验组使用历史证据生成文本及64维adapter。
- 服务器入口：`run_server_test.sh`已切换为20k/500/2 epoch的`igr_generic_q2i`训练，输出写入独立目录。
- 结论：`[严格消融已就绪-待CUDA]`。

### [2026-08-27 10:06:59 CST] E057：历史文本Instruction的RetailRocket训练完成

- Epoch 1：train loss `4.647411`，NTP `4.776871`，Q2I `-0.647297`，HR@1/5/10 `0.040/0.068/0.076`，MRR `0.051686`，Q2I alignment `-0.949934`。
- Epoch 2：train loss `4.011303`，NTP `4.169609`，Q2I `-0.791529`，HR@1/5/10 `0.074/0.114/0.142`，MRR `0.090466`，Q2I alignment `-0.970612`，legal SID rate `1.0`。
- IGR：121个repeat-eligible；epoch 2 repeat recall约`0.64463`，random约`0.68577`，lift约`0.94`，仍未超过随机；recent baseline约`0.66116`。
- 行为分组（epoch 2）：add-to-cart 36条、transaction 25条、view 439条；截图可确认三组指标均正常输出，样本仍以view为主。
- 判断：真实历史文本链训练稳定，NTP下降、Q2I更强且生成指标随epoch上升；但IGR repeat指标仍未证明文本检索优于随机。
- 边界：旧`igr_q2i`日志的scenario和部分实验版本不同，不能直接把当前较高HR归因为文本instruction。
- 下一步：运行无文本、无target-behavior泄漏的严格matched baseline，再判断文本增益。
- 结论：`[真实数据控制流通过-效果归因待严格消融]`。

### [2026-08-27 09:50:42 CST] E056：历史证据Instruction已接入RetailRocket训练

- 新变体：新增`igr_text_q2i`，使用与现有IGR相同的长短历史、registry、Q2I联合loss与constrained generation链路。
- Instruction来源：只读取严格早于target的历史behavior；根据最近5条中的高意图/连续浏览/弱证据生成三类中文公开代理，再经64维稳定hash编码进入模型。
- 防泄漏修正：该变体不使用target behavior作为scenario，统一使用generic scenario；target只作为NTP/Q2I监督标签，不参与instruction构造。
- 评测增强：训练日志新增validation `q2i_alignment`，并继续输出repeat recall、random baseline、lift、HR和MRR，避免只看训练loss。
- 服务器配置：固定既有SID registry，20k train、500 validation、2 epoch、attention history context、IGR top-10；输出单独写入`retailrocket_text_instruction/igr_text_q2i`。
- 本地验证：instruction构造测试、语法、shell和diff检查通过；数值训练待CUDA。
- 结论：`[真实数据链已实现-待CUDA训练与held-out判断]`。

### [2026-08-27 09:50:42 CST] E055：Instruction控制Q2I/IGR的CUDA因果验证通过

- 输出：Q2I alignment从`0.186744`下降到`-1.000000`；初始IGR top-1 indices为`[1,1]`，训练后为`[0,1]`，与expected `[0,1]`完全一致；`controlled_retrieval=True`。
- 控制条件：两条样本共享完全相同的short history、long-history候选池与scenario，仅instruction文本和对应监督target不同；仅训练instruction feature adapter与query adapter。
- 判断：可读instruction不仅能进入模型和产生梯度，而且可通过Q2I对齐学习形成不同query，并控制冻结候选表示上的IGR选择。
- 边界：这是合成可辨识性/因果控制测试，证明机制成立，不代表RetailRocket上自然instruction已经改善排序指标。
- 下一步：接入严格由历史证据构造的RetailRocket instruction，进行真实数据训练与held-out评估。
- 结论：`[Contextual Instruction→Q2I→IGR控制链CUDA通过]`。

### [2026-08-26 22:39:10 CST] E054：可读Instruction实际输入链CUDA通过

- 输出：`feature_shape=(2,64)`，`feature_delta=1.313579`，`logit_delta=1.724638`，`adapter_grad=0.832517`，`retrieval_changed=False`，两组`igr_indices`均为`[3,2]`。
- 通过项：两条不同中文instruction被编码为不同特征；在其他输入相同的因果控制下产生不同logits；instruction adapter获得非零梯度。由此确认文本代理不是报告旁路字段，而是实际进入可微模型。
- 边界：随机初始化、未对齐的instruction adapter未改变离散IGR top-k，这不否定输入链，但尚不能证明instruction具备可控检索语义。
- 下一步：固定两组相同候选池，用两条instruction分别监督两个不同target，只训练instruction adapter和query adapter；验收Q2I alignment下降且IGR top-1分别命中候选0和1。
- 结论：`[CUDA输入与梯度通过-进入语义检索控制验证]`。

### [2026-08-26 22:35:04 CST] E053：可读Instruction真实输入链已实现

- 目的：修复review报告中`Slow LLM文本实际输入=False`暴露的核心缺口，让可读instruction不再只是旁路展示字段。
- 实现：新增确定性、跨进程稳定的signed feature hashing文本编码器；中文字符/二元组与英文词被编码为L2归一化dense feature，再经现有`instruction_feature_adapter`进入reasoning prompt、Q2I query、IGR和decoder。
- 审计：新增`ContextualInstruction`和JSONL `InstructionStore`，同时保存文本、证据、source和version；显式标记为`deterministic_public_proxy`，不冒充论文私有Slow LLM。
- CUDA fixture：保持history、target、scenario和模型完全相同，仅替换两条中文instruction；检查feature/logit差异、adapter梯度和IGR indices，并打印`retrieval_changed`但不强制随机初始化下top-k必须改变。
- 本地验证：51项测试中30项通过、21项因本机无PyTorch/NumPy跳过；新增hash确定性/文本敏感性/store round-trip测试通过，Python/Shell语法及diff检查通过。
- 服务器入口：`run_server_test.sh`已改为执行`validate_contextual_instruction_input.py --device cuda`。
- 结论：`[真实代理文本输入链完成-待CUDA控制流验收]`。

### [2026-08-26 22:35:04 CST] E052：增强证据纠正review-001的归因边界

- IGR证据：top-5全部是约47-49小时前的`view`，均非高意图；4个候选与target SID匹配0/3层，1个匹配1/3层；score集中在约0.9035-0.9220。
- Generation：5个beam全部legal、collision size=1且target hit=False；第一层均命中target的185，细粒度层未命中。
- 重要纠正：最近高意图事件位于short history，而IGR检索的是与其互斥的long history，因此“top-k没取回该高意图事件”不能直接判定为IGR错误。
- 确认问题：reasoning文本只用于review展示，模型实际仍是learned fallback + history context；在文本未输入的前提下，不能要求IGR遵循页面中的自然语言指令。
- 决策：停止围绕本case的IGR score调参，优先打通可读reasoning文本的真实输入和可微影响验证。
- 结论：`[归因已纠正-核心缺口转为instruction实际输入]`。

### [2026-08-26 22:25:47 CST] E051：review-001暴露Alignment、Retrieval与Generation脱节

- Reasoning：41条历史含38 view、2 add-to-cart、1 transaction，最近高意图事件=1；“最近存在高意图行为”有证据，但instruction过于泛化，未指出具体action和时间距离。
- Q2I：`alignment_loss=-0.985537`，对单样本可解读为cosine proxy约`0.985537`，表明q2i query与target embedding数值对齐很强。
- IGR：top-5 score约0.9035-0.9220，但未取回目标SID `[185,73,113]`；表明query-target对齐强不等于在长历史中检索正确。
- Generation：5个beam全部legal且collision size=1，但target hit全为False；所有beam的第一层均为185，与target粗粒度前缀一致，第2/3层细粒度区分失败。
- 链路判断：本例显示`Q2I高对齐 -> IGR未取回 -> beam仅命中粗前缀`，是一个典型的alignment/retrieval/generation目标脱节样例。
- 报告缺口：当前IGR表只有SID和score，不含历史事件behavior、时间距离、匿名item和与target的SID层命中数，无法人工判断“检索证据是否相关”。
- 处理：为每个IGR候选增加behavior、hours-before-target、high-intent、匿名item和`sid_level_matches_target`；Q2I同时显示正向cosine proxy。
- 结论：`[样例review有效-发现跨模块脱节]`。

### [2026-08-26 22:18:12 CST] E050：Review JSONL结构通过，人工可读性待修正

- 输出：5条`llm_cross_cases.jsonl`已在CUDA服务器生成；截图可确认reasoning proxy、actual conditioning、IGR、Q2I、generation和review questions字段均存在。
- 读图边界：截图中单行JSON过长且字号过小，无法可靠辨认每条score、SID和汇总命中数，不根据图片补造数值。
- 发现：JSONL适合机器处理，但不适合终端截图或逐例人工review；“已导出”不等于“可验收”。
- 处理：保留JSONL，同时自动生成同名Markdown报告；每条case拆分为基本信息、行为证据、IGR Top-K表、Q2I、Constrained Beam表和中文checklist。
- 结论：`[数据结构通过-展示层已修正待重新导出]`。

### [2026-08-26 22:06:07 CST] E049：LLM交叉链路review样例导出器已实现

- 目的：将高优先级模块从一行聚合指标转为可人工检查的中间轨迹。
- 样例内容：匿名用户/目标、历史行为证据、中文结构化reasoning代理、模型实际conditioning、IGR top-k index/score/SID、Q2I alignment信息、beam score/合法性/命中/碰撞数。
- 边界透明：reasoning字段显式标记为`deterministic_public_proxy_not_slow_llm_output`；另记录模型实际使用的learned fallback + history attention，不把人工规则文本伪装成模型输入。
- 安全：用户和item ID在review文件中使用SHA-256前12位匿名；保留SID和聚合轨迹用于技术检查。
- 输出：`outputs/review/llm_cross_cases.jsonl`，默认5个固validation样例，并提供三个固review问题。
- 结论：`[完成-待CUDA导出]`。

### [2026-08-26 22:06:07 CST] E048：多模态Q-Former CUDA验证通过并冻结

- 输出：`item_shape=(16,32)`，`query_shape=(16,8,64)`，`loss=1.529825 -> 0.000533`，`text_delta=0.232014`，`image_delta=0.696091`。
- 判断：前向、反向、固定batch overfit、残差Q-Former形状和双模态影响全部通过；loss远低于20%验收门槛。
- 决策：按E047优先级冻结推荐特有多模态编码方向，不继续Qwen3/CLIP权重、I2I或SID调优。
- 结论：`[结构与GPU控制流完成-已冻结]`。

### [2026-08-26 21:57:50 CST] E047：将LLM/Agentic Search可迁移性设为最高优先级

- 项目定位：OxygenREC系列是转向其他LLM算法和Agentic Search项目的过渡项目，不以深挖推荐特有工程为主要产出。
- 高优先级：Contextual Reasoning Instruction、IGR/Retrieval、SA-GCPO/RL、OxygenREC-v2 trajectory optimization；这些模块要保留中间输出、可解释轨迹和样例review。
- 中高优先级：Q2I semantic alignment，重点学习query-document/item representation alignment。
- 低投入模块：SID/RQ、基础Encoder-Decoder GR和工业多场景recommendation只做GPU结构/控制流验收，不再搜索参数或追求论文私有指标。
- 当前多模态决策：只完成E046 CUDA smoke验证结构work；不继续投入真实Qwen3/CLIP权重、I2I大规模训练或SID调优。
- 质量验收：为高优先级模块导出“输入→instruction/query→retrieval/reward→candidate/trajectory→结果”的代表性样例，供人工review。
- 结论：`[项目优先级已冻结-LLM交叉内容优先]`。

### [2026-08-26 21:54:29 CST] E046：OxygenREC-v1多模态融合结构已实现

- 目的：补齐v1 GPU结构复现中最明显的商品编码缺口。
- 实现：新增`MultimodalItemEncoder`，接收上游文本/图像token，分别线性投影并加modality embedding，通过可学习query tokens、多层cross-attention Q-Former、残差query递推和MLP输出商品向量。
- 边界：Qwen3/CLIP作为可替换上游编码器边界；RetailRocket没有原始标题/图像，本次不伪造多模态数据或声称预训编码器权重复现。
- 验证：新增shape、gradient、双模态影响和batch validation测试；CUDA fixture要求固定batch MSE降至初值20%以下，且text/image ablation delta均非0。
- 本地验证：`49 tests`，28项通过，21项因本机无PyTorch/NumPy跳过；语法和diff检查通过。
- 结论：`[结构完成-待CUDA]`。

### [2026-08-26 21:54:29 CST] E045：纠正复现目标与阶段顺序

- 用户确认目标：先在GPU侧完整复现OxygenREC-v1公开的结构和方法；论文未开源数据、模型、权重和服务可以使用明确代理，最后抽取样例做质量review，不要求100%还原私有系统。
- 纠正：E044的GPU reference/NPU compare代码保留但暂缓，不再作为当前主线；在v1 GPU结构/方法验收前不进入NPU。
- GPU验收定义：论文模块有对应实现，可执行前向/反向和端到端链路，私有依赖有显式代理/阻塞标注，有定量测试和定性样例review。
- 结论：`[路线已纠正-GPU v1优先]`。

### [2026-08-26 21:45:31 CST] E044：GPU reference导出与跨设备对齐检查已实现

- 目的：进入GPU→NPU迁移准备，先冻结可审计GPU数值基准，而不是仅保存一行指标。
- GPU导出：新增`export_gpu_reference.py`，固定Base epoch-2 checkpoint、registry版本、checkpoint SHA-256、32条确定validation输入、三层logits、loss、beam SID和beam scores。
- 跨设备比较：新增`compare_device_reference.py`，先校验checkpoint hash和registry版本，再检查logits allclose、max absolute error、loss delta和beam完全一致。
- NPU接口：当`--device npu:0`时显式导入`torch_npu`；默认容差`atol=rtol=5e-3`，失败时不会伪装成通过。
- 当前执行：`run_server_test.sh`只导出GPU reference，不再重跑已冻结的行为试验。
- 本地验证：Python/Shell语法、47项测试和diff检查通过；数值路径待CUDA、NPU实测。
- 结论：`[完成-GPU reference待导出]`。

### [2026-08-26 21:45:31 CST] E043：5000条复评推翻行为强度小样本正收益

- 配置：同一5000条validation cohort，直接比较Base和`behavior_strength_decay` epoch-2 checkpoint。
- Base总体：HR@1 `0.1446`，HR@5 `0.2428`，MRR `0.181640`。
- Strength+Decay总体：HR@1 `0.1350`，HR@5 `0.2274`，MRR `0.168843`。
- Base transaction（115）：HR@1 `0.286957`，HR@5 `0.373913`，MRR `0.321304`；Strength+Decay：`0.156522/0.304348/0.212319`。
- Base add-to-cart（295）：HR@1 `0.200000`，HR@5 `0.298305`，MRR `0.241921`；Strength+Decay：`0.193220/0.322034/0.237232`。
- Base view（4590）：HR@1 `0.137473`，HR@5 `0.235948`，MRR `0.174267`；Strength+Decay：`0.130719/0.219390/0.163359`。
- 发现：扩大transaction由26到115后，先前显著正收益消失并转为明显下降；E041是小样本波动，不是稳定高价值行为收益。
- 判断：历史behavior embedding、1/2/3 target weighting和0.05衰减的控制流已完成，但RetailRocket公开代理效果失败。
- 决策：冻结行为方向，不再调权重/衰减；转入GPU/NPU数值对齐准备。
- 结论：`[控制流完成-公开代理收益失败]`。

### [2026-08-26 21:03:35 CST] E042：行为强度效果的5000条eval-only复评已配置

- 问题：E041中transaction只有26条，虽然提升明显，但少数样本波动可能较大。
- 处理：不重训、不调权重；直接加载Base和`behavior_strength_decay` epoch-2 checkpoint，在同一5000条validation cohort上做eval-only。
- 判定：重点比较transaction/add-to-cart的sample count、HR@1/5、MRR和NDCG；同时保留总体指标作为代价。
- 决策规则：本轮后冻结行为强度/衰减参数，不做网格搜索。
- 结论：`[完成-待CUDA]`。

### [2026-08-26 21:03:35 CST] E041：行为强度+衰减改善高价值transaction排名

- 配置：`behavior_strength_decay`，seed 17、20000 train、1000 validation、2 epochs、beam=5。
- 总体epoch 2：`train_loss=3.838939`，HR@1 `0.127`，HR@5 `0.241`，MRR `0.167833`，legal SID rate `1.0`。
- Add-to-cart（64）：HR@1 `0.203125`，HR@5 `0.390625`，MRR `0.273958`，NDCG约`0.303049`。
- Transaction（26）：HR@1 `0.192308`，HR@5 `0.346154`，MRR `0.250000`，NDCG约`0.273970`。
- View（910）：HR@1 `0.119780`，HR@5 `0.226374`，MRR `0.158022`，NDCG约`0.174984`。
- 发现：相比普通Behavior，总体HR@1/MRR回升，HR@5基本持平；transaction的HR@1/5和MRR继续显著提升。
- 与Base的边界：总体HR@1/MRR仍略低于Base，但HR@5更高；这是用总体头部精度换取高价值行为覆盖的取舍，不是全指标胜出。
- 结论：`[高价值行为正向-样本量待扩大]`。
- 下一步：用已有checkpoint扩大到5000条validation复评。

### [2026-08-26 20:49:53 CST] E040：行为强度加权与时间衰减代理已实现

- 目的：针对E039显示的91% view样本主导问题，增强高意图行为的训练信号，并显式衰减较旧历史行为embedding。
- 固定工程代理：target loss强度`view/cart/transaction = 1/2/3`；历史behavior embedding按与最近交互的序列距离使用`exp(-0.05 * age)`衰减。
- 实现：NTP支持batch内sample weights并按权重和归一化；新增`behavior_strength_decay` variant及config持久化字段。
- 对照：继续使用seed 17、冻结SID registry、20000 train、1000 validation、2 epochs、beam=5；不搜索权重或衰减系数。
- 边界：1/2/3和0.05是公开数据可审计的单组工程设定，不是论文披露的生产超参数。
- 本地验证：`47 tests`，28项通过，19项因缺PyTorch/NumPy跳过；语法和diff检查通过。
- 结论：`[完成-待CUDA]`。

### [2026-08-26 20:49:53 CST] E039：Behavior分类收益与样本失衡定位

- 配置：直接评测Base/Behavior epoch-2 checkpoint，同一1000条validation cohort。
- 样本分布：view=910（91%），add-to-cart=64（6.4%），transaction=26（2.6%）。
- Base add-to-cart：HR@1 `0.2500`，HR@5 `0.390625`，MRR `0.296094`；Behavior：`0.203125/0.390625/0.267969`。
- Base transaction：HR@1 `0.153846`，HR@5 `0.192308`，MRR `0.163462`；Behavior：`0.153846/0.269231/0.205128`。
- Base view：HR@1 `0.127473`，HR@5 `0.215385`，MRR `0.160769`；Behavior：`0.114286/0.230769/0.154176`。
- 发现：transaction的HR@5/MRR明显提升；view的HR@5上升但HR@1/MRR下降；add-to-cart的HR@5持平而HR@1/MRR下降。
- 判断：行为序列信号对稀少高意图transaction有正向作用，但聚合优化被view严重主导；直接否定behavior条件不准确。
- 结论：`[信号有效-样本失衡]`。
- 下一步：用单组预定行为强度权重和时间衰减复测，不做网格搜索。

### [2026-08-26 20:37:07 CST] E038：分行为eval-only诊断已配置

- 问题：E037总体HR@5提升，但HR@1/MRR下降，聚合指标无法判断行为条件对view、add-to-cart、transaction的影响是否一致。
- 处理：在统一validation函数中按target behavior分组，输出每组sample count、HR@1/5、MRR和NDCG。
- 效率：`run_server_test.sh`改为直接加载Base/Behavior已有epoch-2 checkpoint做eval-only，不重复训练。
- 判定：先检查三种行为的样本不平衡与收益方向；再决定是否引入行为强度权重，避免盲目设置view/cart/transaction权重。
- 结论：`[完成-待CUDA]`。

### [2026-08-26 20:36:34 CST] E037：行为序列条件对总体排名影响混合

- 配置：Base与Behavior共用冻结SID registry、seed 17、20000 train、1000 validation、2 epochs、beam=5。
- Base epoch 2：`train_loss=3.778815`，HR@1 `0.135`，HR@5 `0.226`，MRR `0.169500`，legal SID rate `1.0`。
- Behavior epoch 2：`train_loss≈3.840022`，HR@1 `0.121`，HR@5 `0.242`，MRR `0.162783`，legal SID rate `1.0`。
- 发现：Behavior的HR@5增加0.016，但HR@1降低0.014、MRR降低约0.0067，train loss也略高；行为条件改变了候选排序，但没有带来一致头部收益。
- 判断：基础behavior embedding不能判定为胜出；需要先分行为检查效果和样本量，不应直接继续调embedding。
- 结论：`[混合-未胜出]`。
- 下一步：用已有checkpoint做分行为eval-only，无需重训。

### [2026-08-26 20:15:33 CST] E036：历史行为序列条件化已实现，待CUDA对照

- 目的：进入行为与多场景阶段，检验模型显式区分历史`view/addtocart/transaction`是否比纯SID序列更好。
- 论文依据：原文的用户意图推理强调长/短期行为序列、不同action type的意图强度、时间衰减和场景感知；本阶段先复现可由RetailRocket直接支持的行为类型序列。
- 数据层：`SIDModelBatch`和`LongShortSIDModelBatch`新增与商品SID一一对齐的history behavior IDs，padding位保持mask隔离。
- 模型层：`OxygenRECConfig.behavior_vocab_size`和独立behavior embedding叠加到每个历史item token；forward/generate/beam/candidate-log-prob入口保持条件一致。
- 实验层：新增`behavior` variant，与`base`共用seed 17、冻结SID registry、20000 train、1000 validation、2 epochs和beam=5；唯一主要差异是history behavior embedding。
- 边界：当前没有伪造Homepage/Feeds/Cart等RetailRocket不提供的工业场景；也尚未加入行为权重和时间衰减，待先验证基础条件信号。
- 本地验证：`46 tests`，28项通过，18项因本机无PyTorch/NumPy跳过；Python/Shell语法和diff检查通过。
- 结论：`[完成-待CUDA]`。
- 下一步：运行`run_server_test.sh`，比较base和behavior的2 epoch train loss、HR@1/5、MRR及legal SID rate。

### [2026-08-26 17:23:09 CST] E035：SA-GCPO三种子复核完成并冻结结论

- 目的：验证E033的held-out小幅改善在seeds 17/23/41上是否稳定。
- seed 17：HR@1 `0.060 -> 0.063`，HR@5 `0.096 -> 0.103`，MRR `0.073900 -> 0.078000`，NDCG `0.079453 -> 0.084229`；paired `39/21/940`。
- seed 23：HR@1约`0.070 -> 0.070`，HR@5约`0.113 -> 0.112`，MRR/NDCG基本持平且小幅波动；paired `28/24/948`。
- seed 41：HR@1约`0.072 -> 0.068`，HR@5约`0.127 -> 0.133`，不同cutoff方向不一致；paired `41/27/932`。
- 可靠读图边界：seed 23/41的部分MRR/NDCG尾数在截图中不够清晰，不补造精确小数；方向判断使用可辨认的HR和paired counts。
- 发现：三个seed的`improved-worsened`均为正（+18/+4/+14），但HR@1/HR@5并未在每个seed、每个cutoff上一致提升。
- 判断：公开代理SA-GCPO存在“弱正向的成对rank信号”，但离线排序收益不稳定；不能声称复现论文私有reward收益。
- 决策：按E034预先规则冻结SA-GCPO方向，不再调reward weight、learning rate、updates或beam width。
- 结论：`[控制流和CUDA闭环完成；公开代理收益弱正向但不稳定]`。
- 下一步：转入OxygenREC-v2 behavior instruction/行为条件预训练，不再继续v1局部调参。

### [2026-08-26 17:13:10 CST] E034：SA-GCPO三种子held-out稳定性复核已配置

- 目的：在不调整reward、learning rate、updates、beam width和样本规模的前提下，检查E033的小幅泛化改善是否稳定。
- 实现：增加`--sample-seed`，同时控制alignment/validation reservoir sampling和SA-GCPO随机性；checkpoint按seed分开保存。
- 协议：固定原`igr_q2i epoch-2`模型，seeds=`[17,23,41]`，每组alignment=200、held-out=1000、updates=20、beam=5、无target注入。
- 执行：`run_server_test.sh`一次依次运行三个seed，无需手工输入三次命令。
- 预先规则：不因单个seed失败调参；根据三组指标delta和`improved-worsened`方向冻结SA-GCPO公开代理结论。
- 结论：`[完成-待CUDA]`。

### [2026-08-26 17:13:10 CST] E033：1000条held-out出现小幅正向泛化证据

- 目的：在更大held-out cohort上检查E031的微小变化。
- 配置：alignment=200，held-out validation=1000，`alignment_target_coverage=26`，`target_injection=none`，`injected_targets=0`。
- 输出：`objective=-0.076661 -> 0.007567`；HR@1 `0.060 -> 0.063`，HR@5 `0.096 -> 0.103`，MRR `0.073900 -> 0.078000`，NDCG `0.079453 -> 0.084229`，legal SID rate保持1.0。
- 成对rank：`improved=39`，`worsened=21`，`unchanged=940`；净改善18条。
- 发现：四个排序指标均小幅上升，且improved明确多于worsened，达到E032预先规定的继续门槛。
- 判断：这是单种子、公开代理reward的小幅正向泛化证据，尚不是稳定收益或论文私有reward复现。
- 结论：`[通过-单种子小幅正向]`。
- 下一步：仅做一次三种子稳定性复核，随后冻结SA-GCPO方向并转入下一模块。

### [2026-08-26 16:44:25 CST] E032：扩大held-out并增加成对排名转移统计

- 问题：E031的200条held-out上HR完全不变，MRR/NDCG只有约0.001的微小增量，样本量不足以支持泛化收益结论。
- 处理：保持200条train alignment cohort和原超参数不变，将held-out validation扩大到1000条。
- 评测增强：除HR/MRR/NDCG外，对每条held-out样本记录首个命中rank，输出`improved/worsened/unchanged`成对转移数。
- 判定标准：只有指标增量与`improved > worsened`同时出现，才进入多种子稳定性复核；否则冻结公开reward的效果状态，不再循环调参。
- 结论：`[完成-待CUDA]`。
- 下一步：运行更新后的`run_server_test.sh`。

### [2026-08-26 16:44:25 CST] E031：无注入SA-GCPO在held-out上仅有微小排名变化

- 目的：使用独立train alignment cohort更新，仅在held-out validation做前后评测。
- 配置：alignment=200，held-out validation=200，`alignment_target_coverage=26`，`target_injection=none`，`injected_targets=0`。
- 输出：`objective=-0.076661 -> 0.007567`；HR@1 `0.065 -> 0.065`，HR@5 `0.105 -> 0.105`，MRR `0.079333 -> 0.080750`，NDCG `0.085705 -> 0.086795`，legal SID rate保持1.0。
- 发现：目标函数明显上升，但held-out HR无变化，MRR/NDCG只有极小增量。
- 判断：可以确认独立cohort上没有明显退化，但不足以宣称泛化收益成立。
- 结论：`[中性-证据不足]`。
- 下一步：扩大held-out并统计成对rank转移，不继续盲目调参。

### [2026-08-26 12:00:37 CST] E030：纠正SA-GCPO同cohort训练/评测泄漏

- 问题：E028/E029均在同一200条validation cohort上执行SA-GCPO更新后再评测，指标提升属于in-sample拟合，不是未见样本泛化收益。
- 判断：E029排除了target注入是唯一改善来源，但仍不能将HR/MRR/NDCG提升解读为有效离线结论。
- 处理：将脚本拆成两个时间切分：从train split固定采样200条alignment cohort做SA-GCPO，另用200条validation split仅做before/after held-out评测。
- 协议：alignment与validation保持checkpoint原时间边界、sample seed、registry、beam width和指标一致；默认继续`target_injection=none`。
- 本地验证：Python/Shell语法与`git diff --check`通过。
- 结论：`[实验协议已纠正-待CUDA]`。
- 下一步：只以held-out validation的前后差异判断SA-GCPO公开代理是否有泛化收益。

### [2026-08-26 12:00:37 CST] E029：无target注入SA-GCPO同cohort指标提升

- 目的：排除E028的teacher target注入对指标改善的单因影响。
- 配置：与E028相同checkpoint/variant/200条cohort/20 updates，`target_injection=none`。
- 输出：`target_coverage=21`，`injected_targets=0`，`objective=-0.083828 -> 0.004480`；HR@1 `0.065 -> 0.085`，HR@5 `0.105 -> 0.125`，MRR `0.079333 -> 0.099333`，NDCG `0.085705 -> 0.105705`，legal SID rate保持1.0。
- 发现：无teacher候选时仍有提升，幅度小于E028；说明teacher注入放大了收益，但不是唯一来源。
- 结论：`[通过-无注入同cohort，尚非泛化证据]`。
- 后续纠正：见E030，同cohort更新和评测存在数据泄漏。

### [2026-08-26 11:25:24 CST] E028：RetailRocket teacher-augmented SA-GCPO指标提升

- 目的：验证固定RetailRocket validation cohort上的SA-GCPO前后指标闭环。
- checkpoint：自动选中`retailrocket_attention_context_instruction/igr_q2i/epoch-2.pt`，variant=`igr_q2i`，validation=200。
- 输出：`injected_targets=179`，`objective=0.265028 -> 0.399371`；HR@1 `0.065 -> 0.100`，HR@5 `0.105 -> 0.165`，MRR `0.079333 -> 0.123083`，NDCG `0.085705 -> 0.135513`，legal SID rate `1.0 -> 1.0`。
- 发现：四项排序指标均提升，但原old-policy beam只覆盖21/200个target；179/200（89.5%）样本依赖target SID注入。
- 判断：这证明teacher-augmented公开代理SA-GCPO能改善同cohort排序，但无法单独归因于纯on-policy rollout；如直接声称“SA-GCPO真实离线收益已复现”会过度。
- 处理：增加`--target-injection none|always`开关，默认改为`none`；输出原始`target_coverage`，并按模式分开保存checkpoint，避免覆盖teacher结果。
- 结论：`[通过-teacher代理，不是on-policy结论]`。
- 下一步：相同checkpoint/cohort/hyperparameters运行无target注入对照，判断改善是否依赖teacher candidate。

### [2026-08-26 11:16:51 CST] E027：固定RetailRocket cohort的SA-GCPO对照已实现，待CUDA

- 目的：从合成rollout转入真实RetailRocket validation cohort，比较同一NTP checkpoint在SA-GCPO前后的离线排序指标。
- 实现：新增`train_sa_gcpo_retailrocket.py`，自动发现最近checkpoint，校验SID registry版本，恢复原训练时间边界、variant和长短历史配置。
- 问题：旧`candidate_log_probs()`只传递scenario，会使instruction/IGR候选概率与原checkpoint条件不一致。
- 处理：补齐instruction ID/feature、trigger SID、long-history SID及padding mask的group展开和传递。
- 效率：old-policy beam、公开代理reward和old log-prob只预计算一次，20次update共用同一个固定 cohort，避免重复beam解码。
- 公平性：评测前后使用同一validation样本、SID registry、beam width和指标实现；训练候选组保留old-policy beam，未命中时显式注入target SID并输出数量。
- 边界：reward仍是format、SID-level relative、target-ranking和diversity的公开代理；target注入属于teacher-augmented工程实验，不是论文私有生产rollout。
- 本地验证：Python语法、Shell语法和`git diff --check`通过；本机无PyTorch，真实数据路径待服务器CUDA实测。
- 结论：`[完成-待CUDA]`。
- 下一步：运行`bash run_server_test.sh`，采集objective及before/after HR、MRR、NDCG、legal SID rate。

### [2026-08-26 11:03:15 CST] E026：SA-GCPO端到端rollout CUDA通过

- 目的：验证constrained beam候选、四类公开代理reward、old/current policy log-prob重算和SA-GCPO更新能否形成可微闭环。
- 配置：2个样本，每个4个合法beam候选，3层SID；format/relative/ranking/diversity使用公开数据可验证代理。
- 输出：`beams=(2, 4, 3)`，`reward_min=1.177778`，`reward_max=3.177778`，`objective=0.256290 -> 0.724568`。
- 发现：beam形状和reward范围符合fixture设计，策略更新后objective上升0.468278。
- 判断：候选生成→reward mapping→概率重算→SA-GCPO backward/update整链路已完成CUDA实测；不再是单独数学fixture。
- 边界：本轮仍是小型合成rollout，ranking reward不是论文的私有统一排序服务，不能解读为RetailRocket离线收益。
- 处理：将Reward Mapping和SA-GCPO rollout状态更新为`[代理-实测]`和`[完成-实测]`。
- 结论：`[通过-端到端CUDA实测]`。
- 下一步：将rollout接入固定RetailRocket validation cohort和已冻结SID registry，比较NTP checkpoint与SA-GCPO后的HR/MRR/NDCG及合法SID率。

### [2026-08-26 10:45:12 CST] E025：SA-GCPO公式4-9 CUDA完整通过

- 目的：补齐E024未触发的真实目标reward阈值分支。
- 输出：`objective=0.144860 -> 0.630661`，`suppressed=2`，`ratio_min=0.367894`，`ratio_max=3.547215`。
- 发现：恰好两个“优势为正但reward低于真实目标item”的候选被公式8置零；soft gate下objective稳定提升。
- 判断：group advantage、importance ratio、真实reward阈值、非对称温度、soft gate和backward均完成CUDA实测。
- 处理：停止纯数学fixture；实现公开Reward Mapping四组件及固定beam候选的可微log-prob重算，进入端到端rollout验证。
- 结论：`[通过-数学核心实测]`。
- 下一步：验证constrained beam→format/relative/ranking/diversity reward→old/current policy ratio→SA-GCPO update整链路。

### [2026-08-26 10:41:44 CST] E024：SA-GCPO CUDA首次验证仅部分覆盖

- 目的：验证论文公式4-9的soft gate、importance ratio、reward threshold和backward。
- 输出：`objective=0.269641 -> 0.878700`，`ratio_min=0.367894`，`ratio_max=3.547215`，`suppressed=0`。
- 发现：objective显著提高且ratio离开1，证明soft gate与梯度优化链路可运行；但合成fixture没有触发公式8的真实目标reward阈值分支。
- 判断：只能标记soft gate/ratio/backward通过，不能宣称SA-GCPO全部核心分支已完成CUDA验证。
- 处理：把每组第二高reward设置为“标准化优势为正但低于目标item reward”，要求恰好抑制两个候选，并加入`suppressed == 2`硬断言。
- 结论：`[部分通过-fixture覆盖不足]`。
- 下一步：短时复测阈值分支；通过后开始Reward Mapping公开代理和beam rollout接口。

### [2026-08-26 10:35:11 CST] E023：属性检索未过门槛并切换到SA-GCPO

- 目的：验证冻结RetailRocket属性side feature是否比纯SID表示提供更好的长历史检索信号。
- 配置：20条短历史属性向量均值查询50条长历史属性向量，cosine top-5；与解析random和recent比较。
- 输出：用户提供 `repeat_lift=0.992765`，未超过预先规定的1.0门槛；其余尾数未提供，不做推断。
- 发现：属性side information比先前部分学习检索器更接近随机，但仍没有形成可确认优势。
- 判断：纯SID、trigger、mean、attention和冻结属性五条公开代理均未稳定超过随机；继续围绕RetailRocket IGR追求正收益已缺乏证据。
- 决策：冻结IGR状态为“论文控制流复现完成，公开代理收益复现失败”，停止该方向实验，切换到v1多场景偏好对齐。
- 论文核对：Reward Mapping包含format、relative、ranking、diversity四类奖励；SA-GCPO使用group normalized advantage、目标item真实reward阈值、sigmoid soft gate和正负非对称温度（论文公式4-9）。
- 处理：实现独立SA-GCPO数学核心及CUDA验证；工业统一ranking service不可得，后续公开reward必须单独标注为代理。
- 结论：`[IGR公开收益路线终止-SA-GCPO启动]`。
- 下一步：先验证soft gate、reward threshold、importance ratio和backward，再接生成beam与公开reward mapping。

### [2026-08-26 10:30:41 CST] E022：Attention大样本复核失败并终止行为SID调参

- 目的：在2,000 validation上复核E021的epoch 1短暂越过随机是否稳定。
- 输出：epoch 1 `repeat≈0.679121`，epoch 2 `repeat≈0.672527`；共同随机基线约0.697353、recent约0.703297，eligible=455；两个epoch均低于随机。
- 发现：E021在107个eligible样本上的`lift>1`是小样本波动，scenario attention没有形成稳定检索优势。
- 判断：trigger、masked mean、scenario attention和checkpoint epoch均已受控验证，继续在纯行为SID query pooling上调参会形成局部死循环。
- 决策：正式停止该路线，不再增加pooling变体、epoch或Q2I权重搜索。
- 新方向：补全论文item表示中缺失的side information。当前Q2I/IGR只使用SID embedding，而论文表示包含item ID、side-info与文本；RetailRocket已有冻结的256维属性hash代理向量。
- 处理：先进行无需训练的属性side-information检索门槛实验：用20条短历史属性均值查询50条长历史属性，和相同random/recent基线比较。只有该表示lift>1才进入模型融合。
- 结论：`[终止旧路线-切换表示方法]`。
- 下一步：若属性检索有效，将side feature接入Q2I target和IGR long item adapter；若无效，停止RetailRocket IGR收益追求并转入下一论文模块。

### [2026-08-26 10:21:33 CST] E021：Scenario-Attention首次越过随机但随后退化

- 目的：验证scenario-conditioned attention能否比masked mean更有效地选择短历史并形成个性化reasoning prompt。
- 配置：attention-context Q2I-only→IGR+Q2I两阶段，各20K×2 epochs；500 validation，eligible=107；迁移21个tensor。
- 输出边界：终端照片中部分训练loss尾数不清晰，不用于精确比较；identity assertion均通过。
- 可确认输出：
  - IGR+Q2I epoch 1：`repeat≈0.738318`，`random≈0.731666`，`recent≈0.785047`，`lift≈1.009996`；
  - IGR+Q2I epoch 2：`repeat≈0.719626`，`random≈0.731666`，`recent≈0.785047`，`lift≈0.983545`；HR@1约0.042、HR@5约0.086、MRR约0.059。
- 发现：epoch 1首次略微超过随机，证明attention存在学习信号；继续联合训练到epoch 2后检索质量下降，且两个epoch都未超过recent baseline。
- 判断：可能存在NTP生成目标、Q2I对齐和离散检索选择之间的优化冲突或过拟合。不能事后只挑epoch 1宣称成功，也不能基于107个eligible样本确认退化。
- 处理：冻结训练动作，分别加载epoch 1/2 checkpoint，在相同2,000 validation上复评repeat/random/recent/lift。
- 结论：`[首次越过随机-稳定性待验证]`。
- 下一步：若大样本仍表现为epoch 1优于epoch 2，则增加预先定义的retrieval early stopping，并研究分阶段冻结/降低Q2I权重；否则视为小样本波动。

### [2026-08-25 17:40:55 CST] E020：Masked-Mean History Context未过门槛

- 目的：验证完整短历史均值是否能提供比单trigger更充分的个性化reasoning proxy。
- 配置：scenario+trigger+20条短历史masked mean，经context adapter；Q2I-only→IGR+Q2I两阶段，各20K×2 epochs；500 validation。
- 输出边界：终端照片可确认迁移15个tensor；生成指标尾数模糊，按近似值记录。
- 可确认输出：IGR+Q2I epoch 2 的HR@1约0.05、HR@5约0.078、MRR约0.056；`repeat=0.710280`，`random=0.731666`，`recent=0.785047`，`lift≈0.970772`，eligible=107。
- 发现：masked mean没有超过trigger版本，也仍低于随机和recent；简单平均可能稀释短历史中的当前意图。
- 判断：问题不再是“上下文数量不足”，而是上下文选择方式缺乏针对当前scenario的权重。继续堆epoch缺少依据。
- 处理：实现scenario-conditioned attention，以scenario embedding查询20条短历史，mask padding后加权汇聚为reasoning prompt；该attention可通过Q2I获得直接梯度。
- 结论：`[失败-平均池化不适用]`。不报告为IGR有效复现。
- 下一步：训练attention-context Q2I→IGR；若lift仍不超过1，停止纯行为SID代理，转向离线文本/属性instruction表示。

### [2026-08-25 17:32:19 CST] E019：Trigger模型扩大验证复核

- 目的：用更多eligible样本判断E018中接近1的repeat lift是否只是小样本噪声。
- 配置：不重训，加载同一trigger-conditioned IGR+Q2I epoch-2 checkpoint；2,000 validation。
- 精确输出：HR@1=0.0535，HR@5=0.1025，MRR=0.070358；`repeat=0.679121`，`random=0.697353`，`recent=0.703297`，`lift=0.973855`，eligible=455。
- 发现：eligible由107增至455后，lift仍低于1；repeat比随机低约1.82个百分点，比recent低约2.42个百分点。
- 判断：trigger方向相对无个性化query有显著改善，但仍未达到检索质量门槛。新验证集上的HR不能与旧500样本HR直接横比。
- 处理：不再单纯增加trigger版本epoch；实现完整短历史的masked mean pooling，经adapter加入reasoning prompt，同时保留scenario和trigger。
- 方法边界：这是公开数据的contextual instruction代理，只使用严格早于target的短历史，不是论文Slow LLM生成的文本reasoning。
- 结论：`[稳定低于随机-进入更强上下文代理]`。
- 下一步：重新训练history-context Q2I-only并初始化IGR+Q2I，仍以random/recent/lift作为首要门槛。

### [2026-08-25 17:27:06 CST] E018：Trigger-Conditioned Q2I→IGR

- 目的：验证最近短历史trigger能否补充用户级当前意图，使Q2I检索空间优于无个性化的三类scenario query。
- 配置：先训练trigger-conditioned Q2I-only 20K×2 epochs，再迁移13个相关tensor并训练IGR+Q2I 20K×2 epochs；500 validation，eligible=107。
- 输出边界：结果来自用户提供的终端照片，小数尾数以图片可辨识范围记录；identity assertion均为零或浮点误差量级。
- 可确认输出：IGR+Q2I epoch 2 的HR@5约0.078、MRR约0.053；`repeat=0.719626`，`random=0.731666`，`recent=0.785047`，`lift≈0.98354`。
- 对比：相对E017的`repeat=0.598131/lift=0.817492`，绝对repeat recall提高约0.1215，lift提高约0.166。
- 发现：个性化trigger显著改善检索，证明E017关于query缺乏用户上下文的定位成立；但点估计仍略低于随机，且低于recent约6.5个百分点。
- 判断：eligible仅107时，0.7196与随机0.7317的约1.2个百分点差距可能由抽样噪声造成，不能据此立即停止或宣称通过。
- 处理：不重新训练，加载同一epoch-2 checkpoint，将validation扩大到2,000，重新计算repeat/random/recent/lift。
- 结论：`[显著改善-尚未过门槛]`。trigger是有效方向，但当前证据量不足。
- 下一步：若扩大验证后lift仍稳定低于1或明显弱于recent，再实现基于完整短历史的contextual instruction代理；若超过1，则进入多种子确认。

### [2026-08-25 17:19:48 CST] E017：Q2I初始化后的IGR仍失败

- 目的：验证IGR低质量是否主要来自query/item adapter随机初始化。
- 配置：从Q2I-only epoch-2迁移13个SID/instruction/query/item相关tensor，再训练IGR+Q2I 20K×2 epochs。
- 输出：epoch 2 HR@1约0.034、HR@5约0.056、MRR约0.042833；`repeat=0.598131`，`random=0.731666`，`recent=0.785047`，`lift=0.817492`，eligible=107。
- 发现：两阶段初始化后repeat recall和生成指标均未改善，反而低于原IGR+Q2I。
- 判断纠正：随机初始化不是主因。当前公开代理仅提供view/cart/transaction三种scenario和同一个default reasoning embedding，因此全数据只有三类query向量，缺少论文Contextual Reasoning Instruction所包含的用户级当前意图；Q2I无法用三向量同时精确对齐大量不同目标商品。
- 处理：使用论文支持的可选trigger item，把每个样本最近一个严格早于target的短历史SID注入scenario instruction；先重新训练个性化Q2I-only，再初始化IGR+Q2I。
- 结论：`[失败-定位到instruction代理缺失]`。后续实验若有改善，应归因于trigger-conditioned公开代理，不能称为复现了Slow LLM reasoning instruction。
- 下一步：检查trigger-conditioned Q2I/IGR的repeat lift；若仍低于1，停止该简化代理并实现更丰富的离线上下文instruction特征。

### [2026-08-25 17:12:17 CST] E016：IGR 与随机/最近历史基线

- 目的：判断 E015 中约0.63的 SID repeat recall 是否真正优于无学习基线。
- 配置：直接加载 E015 的 epoch-2 checkpoint，不重新训练；500条相同验证样本，top-k=5，eligible=107。
- 精确输出：
  - IGR：`repeat=0.626168`，`recent=0.785047`，`random=0.731666`，`lift=0.855812`，HR@5=0.076，MRR=0.052800；
  - IGR+Q2I：`repeat=0.635514`，`recent=0.785047`，`random=0.731666`，`lift=0.868585`，HR@5=0.060，MRR=0.044367。
- 发现：两种IGR均低于随机无放回期望和最近5条基线；Q2I仅带来约0.94个百分点绝对repeat recall提升。
- 判断：当前公开代理检索器确实无效，而不仅是验证方差导致“收益不显著”。继续原配置扩大训练没有充分依据。
- 可能原因：IGR query/item adapter 从随机初始化开始；无Q2I的IGR缺少直接检索监督；有Q2I的联合训练在2 epochs内仍不足以建立优于recency的检索空间；SID碰撞进一步污染repeat指标。
- 处理：改为两阶段实验，从Q2I-only epoch-2 checkpoint迁移 SID embedding、scenario/instruction embedding、query adapter和item adapter，再进行IGR+Q2I联合微调；不迁移形状不同的history position参数。
- 结论：`[失败-检索质量门槛]`，当前IGR结果不得作为论文方法已有效复现的证据。
- 下一步：比较Q2I初始化后的repeat lift是否超过1以及是否接近/超过recent baseline；若仍失败，暂停当前SID代理IGR，转向更强item/query表示或融合机制。

### [2026-08-25 15:48:46 CST] E015：Q2I-only 与 IGR Repeat-Recall 诊断

- 目的：区分 Q2I 对齐本身与 IGR 长历史拼接的影响，并确认检索器是否取回重复目标。
- 配置：seed=17，20,000 train、500 validation、2 epochs；变体为 Q2I-only、IGR、IGR+Q2I。
- 输出边界：用户依据图片/OCR提供大致日志，带 `?` 的尾数不作为精确数值；batch/epoch identity assertion 均通过，故损失公式以机器断言为准。
- 可确认输出：
  - Q2I-only epoch 2：HR@1约0.06，HR@5约0.08，MRR约0.065；无长历史，因此 repeat recall 为 `None`；
  - IGR epoch 2：HR@1约0.04，HR@5约0.06，MRR约0.052，SID repeat recall约0.626，eligible=107；
  - IGR+Q2I epoch 2：HR@1约0.045，HR@5约0.06，MRR约0.044，SID repeat recall约0.636，eligible=107；
  - 所有变体合法 SID 比例为1；Nested Tensor warning仅说明 `norm_first=True` 未走该性能优化路径。
- 发现：Q2I-only 没有显示出明显劣化；加入 IGR 后生成指标仍未改善。IGR+Q2I 的 repeat recall 仅比 IGR 高约1个百分点。
- 判断：约0.63的 repeat recall 不能单独证明检索有效。长历史中可能多次出现目标 SID，且冻结 registry 的 item collision 约20%，随机选5条也可能有较高命中率。
- 处理：增加解析随机无放回期望、最近5条历史基线和 IGR/random lift；后续必须同时报告这些基线。
- 结论：`[诊断未完成]`。现有证据不足以把生成指标下降归因于检索召回率、检索噪声或历史融合方式中的某一个。
- 下一步：运行不需要大规模调参的检索基线诊断，再决定是训练检索器、改融合方式还是暂停 IGR 代理路线。

### [2026-08-25 14:33:05 CST] E014：建立独立中文实验日志

- 触发：用户要求把每次输出后发现的问题、判断和解决方法按时间戳留档。
- 发现：原 `复现进度.md` 记录了状态和结果，但对判断演变、错误假设和修正过程记录不足。
- 处理：新增本日志；历史条目标注“具体时间未留存”；以后每轮实验先追加日志，再更新进度摘要。
- 当前待测：Q2I-only、IGR、IGR+Q2I，seed=17，20K train、500 validation、2 epochs；关注 `repeat_recall` 和 `repeat_eligible`。
- 结论：`[流程修正完成]`。

### [2026-08-25 具体时间未留存] E013：三随机种子四组消融

- 配置：seeds=`[17,23,41]`，每组20,000 train、200 validation、2 epochs，matched cohort。
- 聚合输出：

| Variant | HR@1 | HR@5 | MRR | NDCG | NTP loss |
|---|---:|---:|---:|---:|---:|
| Base | 0.0600±0.0180 | 0.0833±0.0058 | 0.0673±0.0148 | 0.0713±0.0125 | 4.2219±0.0125 |
| Instruction | 0.0633±0.0236 | 0.0933±0.0284 | 0.0731±0.0249 | 0.0780±0.0258 | 4.2251±0.0128 |
| IGR | 0.0450±0.0260 | 0.0733±0.0362 | 0.0576±0.0297 | 0.0616±0.0313 | 4.2443±0.0067 |
| IGR+Q2I | 0.0417±0.0289 | 0.0767±0.0379 | 0.0559±0.0322 | 0.0612±0.0335 | 4.2448±0.0072 |

- 发现：Instruction 均值小幅高于 Base，但差值小于标准差；IGR 两组均值下降且方差增大。
- 判断：不能宣称 Instruction 稳定提升；当前公开代理 IGR 没有产生收益。`igr` 无 Q2I 监督，离散 top-k 又阻断 NTP 到检索分数的梯度，因此其 adapter 接近未训练检索器。
- 处理：增加 Q2I-only 变体；增加 SID-level repeat-recall 与 eligible count，直接诊断检索器是否取回长历史中的重复目标。
- 结论：`[负结果-有效诊断]`，不通过调参或只报最好种子掩盖。

### [2026-08-25 具体时间未留存] E012：联合损失符号与日志复核

- 输出截图确认：`train_loss=5.650157`，`ntp_loss=5.665108`，`q2i_loss=-0.074755`，`q2i_weight=0.2`，batch error=`0`，epoch error=`5.877e-08`。
- 发现：实际 Q2I 带负号，之前仅是文字转录遗漏。
- 判断：`5.665108 + 0.2×(-0.074755)=5.650157`；模型、权重和日志聚合均正确。
- 处理：解除 E011 的“待审计”状态；保留 identity assertion 防止未来回归。
- 结论：`[通过-实测]`。经验：遇到聚合算术矛盾先加机器断言，不依据手工转录猜测源码错误。

### [2026-08-25 具体时间未留存] E011：四组20K单种子消融

- 配置：seed=17，20,000 train、50 validation、2 epochs，matched cohort。
- 第2 epoch：Base `HR@5=0.06/MRR=0.045`；Instruction `0.08/0.060`；IGR `0.08/0.049`；IGR+Q2I `0.08/0.049`；合法SID均为1。
- 发现：日志文字中 Q2I 负号一度遗漏，导致表面上联合 loss 算术不一致。
- 初始判断：可能存在服务器源码、损失符号或日志聚合漂移，因此没有直接接受结果。
- 处理：加入逐 batch 和 epoch 两层 loss identity assertion，并用512样本复核。
- 结论：指标样本过少，只能作为中等规模流程测试。

### [2026-08-24 具体时间未留存] E010：真实数据 IGR+Q2I 5K Smoke Test

- 配置：5,000 train、20 validation、1 epoch、short=20、long=50、top-k=5。
- 输出：`train_loss=5.139547`，`ntp=5.235295`，`q2i=-0.478739`，HR@1/5与MRR均为0，`legal_sid_rate=1.0`。
- 发现：`5.235295 + 0.2×(-0.478739)=5.139547`，联合损失关系正确。
- 判断：20条验证样本且只训1 epoch时零命中不能用于方法判断。
- 处理：建立 matched cohort 的 Base/Instruction/IGR/IGR+Q2I 消融。
- 结论：`[通过-真实数据链路]`。


### [2026-08-24 具体时间未留存] E009：Instruction + Q2I + IGR 合成 CUDA 闭环

- 目的：一次验证双 instruction token、Q2I 联合损失、长历史 top-k、padding mask 和 backward。
- 输出：`joint_loss=2.914326 -> -0.162442`，`ntp=0.024518`，`q2i=-0.934801`，`igr_shape=(4,2)`，`instruction_delta=0.206740`。
- 发现：联合 loss 可下降；instruction 改变 logits；IGR 未选 padding。
- 判断：负 Q2I 来自负余弦主项，属于预期；该实验只证明控制流，不证明推荐收益。
- 处理：构造真实数据无重叠 short/long history batch。
- 结论：`[通过-合成实测]`。

### [2026-08-24 具体时间未留存] E008：RQ 2×2 冻结实验

- 关键输出：
  - width=256/K-Means++：`mse=0.001678`，`collision=0.203331`，`unique_sids=16449`，prefix=`[256,7434,16449]`；
  - width=512/random：`mse=0.001584`，`collision=0.325148`，`unique_sids=15001`，prefix=`[512,11890,15001]`。
- 发现：width=512/random 的 MSE 略优，但完整 SID 基数和碰撞更差。
- 判断：推荐 tokenizer 应优先降低 item collision、提高可区分 SID 数，而非追求极小 MSE 差异。
- 处理：冻结 width=256/K-Means++、seed=17 作为 Phase-2 公开代理 registry。
- 结论：`[通过-代理冻结]`；碰撞仍有20.33%，后续必须作为风险报告。

### [2026-08-24 具体时间未留存] E007：width=512/K-Means++ 异常碰撞

- 输出：`represented_items=18733`，`unique_vector_rate=1.0`，`mse=0.003906 -> 0.001596`，`collision_rate=0.444243`。
- 发现：width 增大、MSE 下降，但 collision 反而恶化。
- 判断：K-Means 优化重构误差，不直接优化完整 SID 唯一性；不能只按 MSE 选 tokenizer。
- 处理：补齐 width=256/K-Means++ 与 width=512/random 的2×2控制实验。
- 结论：`[负结果-有解释]`，停止无边界地继续调普通 K-Means。

### [2026-08-24 具体时间未留存] E006：属性代理 RQ width=256

- 输出：`represented_items=18733`，`unique_vector_rate=1.0`，`mse=0.003906 -> 0.001671`，`collision_rate=0.211071`。
- 发现：输入向量没有完全重复；扩大码本显著降低碰撞。
- 判断：剩余碰撞主要来自 RQ 容量与聚类目标，而非完全相同的输入向量。
- 处理：比较 width、初始化、MSE、collision、unique SID 和 prefix occupancy。
- 结论：`[候选可用-实测]`。

### [2026-08-24 具体时间未留存] E005：属性代理 RQ width=64

- 配置：RetailRocket 训练截止前最新属性，256维 signed hashing，width=64。
- 输出：`represented_items=4808`，`mse=0.003906 -> 0.002105`，`collision_rate=0.911606`。
- 发现：接口和训练正常，但91.16%的 item 落入碰撞 SID。
- 判断：该 registry 不可用于后续推荐实验。
- 处理：扩大到 width=256，并确认输入向量是否重复。
- 结论：`[失败-质量门槛]`，实现未失败，SID 唯一性失败。

### [2026-08-24 具体时间未留存] E004：合成向量 CUDA Residual K-Means

- 目的：验证三层分块 PyTorch RQ 实现。
- 输出：`shape=(3,8,16)`，`mse=0.927709 -> 0.006996`，`collision_rate=0.884766`。
- 发现：MSE 显著下降，但 width=8 fixture 碰撞很高。
- 判断：高碰撞由测试容量过小导致，不能据此否定 RQ 实现。
- 处理：转到真实属性代理向量并扩大 width。
- 结论：`[通过-实测，仅正确性 fixture]`。

### [2026-08-24 具体时间未留存] E003：RetailRocket 100K 基础训练

- 目的：验证真实数据读取、时间切分、样本构造、训练、评测和 checkpoint 全链路。
- 配置：100,000 训练样本，3 epochs，单卡 L20；当时使用 frequency-bootstrap SID。
- 输出：3 个 epoch 正常打印，train loss 正常下降；具体数值未因信息安全要求导出。
- 发现：真实数据训练链路可稳定运行。
- 判断：只能证明工程闭环，frequency-bootstrap 不是语义 tokenizer。
- 处理：实现属性代理向量和 CUDA Residual K-Means。
- 结论：`[通过-实测，结构代理]`。

### [2026-08-24 具体时间未留存] E002：Toy CUDA Beam Search

- 目的：验证多候选合法路径和 beam 排序。
- 输出：`[[[1,2,3],[7,8,9]], [[7,8,9],[1,2,3]]]`。
- 发现：每个 beam 都是 registry 中的完整 SID，排序控制流正常。
- 判断：reference beam search 具备正确性，但未优化吞吐和 KV cache。
- 处理：接入真实 RetailRocket 样本。
- 结论：`[通过-实测]`。

### [2026-08-24 具体时间未留存] E001：Toy CUDA 训练与约束生成

- 目的：验证 encoder-decoder forward/backward、单 batch 拟合和合法 SID 生成闭环。
- 配置：CUDA toy batch，三层 SID，PrefixTrie constrained greedy。
- 输出：`loss=2.312395 -> 0.002499`，生成 `[[1,2,3],[7,8,9]]`。
- 发现：loss 可收敛，生成路径均在 trie 中。
- 判断：基础模型、损失、优化器和 greedy 约束控制流正确。
- 处理：进入 beam search 验证。
- 结论：`[通过-实测]`，不能代表真实数据推荐效果。


## 新实验追加模板

```markdown
### [YYYY-MM-DD HH:MM:SS TZ] EXXX：实验名称

- 目的：
- 配置：
- 输出：
- 发现：
- 判断：
- 处理：
- 结论：
- 下一步：
```
