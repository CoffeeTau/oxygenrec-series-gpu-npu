# OxygenREC 复现实验日志

> 用途：按时间顺序记录每次模型实验的配置、输出、发现、判断、处理和后续动作。
> 本文记录实验推理过程；总体完成度与模块状态见 `复现进度.md`。

最后更新：2026-08-26 10:21:33 CST

## 记录规则

1. 新实验插入记录区最前，不覆盖旧结论；判断发生变化时，新增“纠正”记录并指向原记录；
2. 时间统一使用服务器或本地可确认的 `YYYY-MM-DD HH:MM:SS TZ`；
3. 历史对话未保留时分时写“具体时间未留存”，不补造精确时间；
4. 每条至少包含：实验目的、关键配置、输出、发现、判断、处理、结论和下一步；
5. 明确区分论文事实、公开数据代理、工程假设和目标服务器实测；
6. 因公司信息安全只保留允许公开的聚合信息，不记录服务器敏感路径、账号或环境秘密。

## 实验记录（最新在前）

### [2026-08-26 10:21:33 CST] E021：Scenario-Attention首次越过随机但随后退化

- 目的：验证scenario-conditioned attention能否比masked mean更有效地选择短历史并形成个性化reasoning prompt。
- 配置：attention-context Q2I-only→IGR+Q2I两阶段，各20K×2 epochs；500 validation，eligible=107；迁移21个tensor。
- 输出边界：终端照片中部分训练loss尾数不清晰，不用于精确比较；identity assertion均通过。
- 可确认输出：
  - IGR+Q2I epoch 1：`repeat≈0.738318`，`random≈0.731666`，`recent≈0.785047`，`lift≈1.009996`；
  - IGR+Q2I epoch 2：`repeat≈0.719626`，`random≈0.731666`，`recent≈0.785047`，`lift≈0.983545`；HR@1约0.042、HR@5约0.086、MRR约0.059。
- 发现：epoch 1首次略微超过随机，证明attention存在学习信号；继续联合训练到epoch 2后检索质量下降，且两个epoch都未超过recent baseline。
- 判断：可能存在NTP生成目标、Q2I对齐和离散检索选择之间的优化冲突或过拟合。不能事后只挑epoch 1宣称成功，也不能基于107个eligible样本确认退化。
- 处理：冻结训练动作，分别加载epoch 1/2 checkpoint，在相同2,000 validation上复评repeat/random/recent/lift。
- 结论：`[首次越过随机-稳定性待验证]`。
- 下一步：若大样本仍表现为epoch 1优于epoch 2，则增加预先定义的retrieval early stopping，并研究分阶段冻结/降低Q2I权重；否则视为小样本波动。

### [2026-08-25 17:40:55 CST] E020：Masked-Mean History Context未过门槛

- 目的：验证完整短历史均值是否能提供比单trigger更充分的个性化reasoning proxy。
- 配置：scenario+trigger+20条短历史masked mean，经context adapter；Q2I-only→IGR+Q2I两阶段，各20K×2 epochs；500 validation。
- 输出边界：终端照片可确认迁移15个tensor；生成指标尾数模糊，按近似值记录。
- 可确认输出：IGR+Q2I epoch 2 的HR@1约0.05、HR@5约0.078、MRR约0.056；`repeat=0.710280`，`random=0.731666`，`recent=0.785047`，`lift≈0.970772`，eligible=107。
- 发现：masked mean没有超过trigger版本，也仍低于随机和recent；简单平均可能稀释短历史中的当前意图。
- 判断：问题不再是“上下文数量不足”，而是上下文选择方式缺乏针对当前scenario的权重。继续堆epoch缺少依据。
- 处理：实现scenario-conditioned attention，以scenario embedding查询20条短历史，mask padding后加权汇聚为reasoning prompt；该attention可通过Q2I获得直接梯度。
- 结论：`[失败-平均池化不适用]`。不报告为IGR有效复现。
- 下一步：训练attention-context Q2I→IGR；若lift仍不超过1，停止纯行为SID代理，转向离线文本/属性instruction表示。

### [2026-08-25 17:32:19 CST] E019：Trigger模型扩大验证复核

- 目的：用更多eligible样本判断E018中接近1的repeat lift是否只是小样本噪声。
- 配置：不重训，加载同一trigger-conditioned IGR+Q2I epoch-2 checkpoint；2,000 validation。
- 精确输出：HR@1=0.0535，HR@5=0.1025，MRR=0.070358；`repeat=0.679121`，`random=0.697353`，`recent=0.703297`，`lift=0.973855`，eligible=455。
- 发现：eligible由107增至455后，lift仍低于1；repeat比随机低约1.82个百分点，比recent低约2.42个百分点。
- 判断：trigger方向相对无个性化query有显著改善，但仍未达到检索质量门槛。新验证集上的HR不能与旧500样本HR直接横比。
- 处理：不再单纯增加trigger版本epoch；实现完整短历史的masked mean pooling，经adapter加入reasoning prompt，同时保留scenario和trigger。
- 方法边界：这是公开数据的contextual instruction代理，只使用严格早于target的短历史，不是论文Slow LLM生成的文本reasoning。
- 结论：`[稳定低于随机-进入更强上下文代理]`。
- 下一步：重新训练history-context Q2I-only并初始化IGR+Q2I，仍以random/recent/lift作为首要门槛。

### [2026-08-25 17:27:06 CST] E018：Trigger-Conditioned Q2I→IGR

- 目的：验证最近短历史trigger能否补充用户级当前意图，使Q2I检索空间优于无个性化的三类scenario query。
- 配置：先训练trigger-conditioned Q2I-only 20K×2 epochs，再迁移13个相关tensor并训练IGR+Q2I 20K×2 epochs；500 validation，eligible=107。
- 输出边界：结果来自用户提供的终端照片，小数尾数以图片可辨识范围记录；identity assertion均为零或浮点误差量级。
- 可确认输出：IGR+Q2I epoch 2 的HR@5约0.078、MRR约0.053；`repeat=0.719626`，`random=0.731666`，`recent=0.785047`，`lift≈0.98354`。
- 对比：相对E017的`repeat=0.598131/lift=0.817492`，绝对repeat recall提高约0.1215，lift提高约0.166。
- 发现：个性化trigger显著改善检索，证明E017关于query缺乏用户上下文的定位成立；但点估计仍略低于随机，且低于recent约6.5个百分点。
- 判断：eligible仅107时，0.7196与随机0.7317的约1.2个百分点差距可能由抽样噪声造成，不能据此立即停止或宣称通过。
- 处理：不重新训练，加载同一epoch-2 checkpoint，将validation扩大到2,000，重新计算repeat/random/recent/lift。
- 结论：`[显著改善-尚未过门槛]`。trigger是有效方向，但当前证据量不足。
- 下一步：若扩大验证后lift仍稳定低于1或明显弱于recent，再实现基于完整短历史的contextual instruction代理；若超过1，则进入多种子确认。

### [2026-08-25 17:19:48 CST] E017：Q2I初始化后的IGR仍失败

- 目的：验证IGR低质量是否主要来自query/item adapter随机初始化。
- 配置：从Q2I-only epoch-2迁移13个SID/instruction/query/item相关tensor，再训练IGR+Q2I 20K×2 epochs。
- 输出：epoch 2 HR@1约0.034、HR@5约0.056、MRR约0.042833；`repeat=0.598131`，`random=0.731666`，`recent=0.785047`，`lift=0.817492`，eligible=107。
- 发现：两阶段初始化后repeat recall和生成指标均未改善，反而低于原IGR+Q2I。
- 判断纠正：随机初始化不是主因。当前公开代理仅提供view/cart/transaction三种scenario和同一个default reasoning embedding，因此全数据只有三类query向量，缺少论文Contextual Reasoning Instruction所包含的用户级当前意图；Q2I无法用三向量同时精确对齐大量不同目标商品。
- 处理：使用论文支持的可选trigger item，把每个样本最近一个严格早于target的短历史SID注入scenario instruction；先重新训练个性化Q2I-only，再初始化IGR+Q2I。
- 结论：`[失败-定位到instruction代理缺失]`。后续实验若有改善，应归因于trigger-conditioned公开代理，不能称为复现了Slow LLM reasoning instruction。
- 下一步：检查trigger-conditioned Q2I/IGR的repeat lift；若仍低于1，停止该简化代理并实现更丰富的离线上下文instruction特征。

### [2026-08-25 17:12:17 CST] E016：IGR 与随机/最近历史基线

- 目的：判断 E015 中约0.63的 SID repeat recall 是否真正优于无学习基线。
- 配置：直接加载 E015 的 epoch-2 checkpoint，不重新训练；500条相同验证样本，top-k=5，eligible=107。
- 精确输出：
  - IGR：`repeat=0.626168`，`recent=0.785047`，`random=0.731666`，`lift=0.855812`，HR@5=0.076，MRR=0.052800；
  - IGR+Q2I：`repeat=0.635514`，`recent=0.785047`，`random=0.731666`，`lift=0.868585`，HR@5=0.060，MRR=0.044367。
- 发现：两种IGR均低于随机无放回期望和最近5条基线；Q2I仅带来约0.94个百分点绝对repeat recall提升。
- 判断：当前公开代理检索器确实无效，而不仅是验证方差导致“收益不显著”。继续原配置扩大训练没有充分依据。
- 可能原因：IGR query/item adapter 从随机初始化开始；无Q2I的IGR缺少直接检索监督；有Q2I的联合训练在2 epochs内仍不足以建立优于recency的检索空间；SID碰撞进一步污染repeat指标。
- 处理：改为两阶段实验，从Q2I-only epoch-2 checkpoint迁移 SID embedding、scenario/instruction embedding、query adapter和item adapter，再进行IGR+Q2I联合微调；不迁移形状不同的history position参数。
- 结论：`[失败-检索质量门槛]`，当前IGR结果不得作为论文方法已有效复现的证据。
- 下一步：比较Q2I初始化后的repeat lift是否超过1以及是否接近/超过recent baseline；若仍失败，暂停当前SID代理IGR，转向更强item/query表示或融合机制。

### [2026-08-25 15:48:46 CST] E015：Q2I-only 与 IGR Repeat-Recall 诊断

- 目的：区分 Q2I 对齐本身与 IGR 长历史拼接的影响，并确认检索器是否取回重复目标。
- 配置：seed=17，20,000 train、500 validation、2 epochs；变体为 Q2I-only、IGR、IGR+Q2I。
- 输出边界：用户依据图片/OCR提供大致日志，带 `?` 的尾数不作为精确数值；batch/epoch identity assertion 均通过，故损失公式以机器断言为准。
- 可确认输出：
  - Q2I-only epoch 2：HR@1约0.06，HR@5约0.08，MRR约0.065；无长历史，因此 repeat recall 为 `None`；
  - IGR epoch 2：HR@1约0.04，HR@5约0.06，MRR约0.052，SID repeat recall约0.626，eligible=107；
  - IGR+Q2I epoch 2：HR@1约0.045，HR@5约0.06，MRR约0.044，SID repeat recall约0.636，eligible=107；
  - 所有变体合法 SID 比例为1；Nested Tensor warning仅说明 `norm_first=True` 未走该性能优化路径。
- 发现：Q2I-only 没有显示出明显劣化；加入 IGR 后生成指标仍未改善。IGR+Q2I 的 repeat recall 仅比 IGR 高约1个百分点。
- 判断：约0.63的 repeat recall 不能单独证明检索有效。长历史中可能多次出现目标 SID，且冻结 registry 的 item collision 约20%，随机选5条也可能有较高命中率。
- 处理：增加解析随机无放回期望、最近5条历史基线和 IGR/random lift；后续必须同时报告这些基线。
- 结论：`[诊断未完成]`。现有证据不足以把生成指标下降归因于检索召回率、检索噪声或历史融合方式中的某一个。
- 下一步：运行不需要大规模调参的检索基线诊断，再决定是训练检索器、改融合方式还是暂停 IGR 代理路线。

### [2026-08-25 14:33:05 CST] E014：建立独立中文实验日志

- 触发：用户要求把每次输出后发现的问题、判断和解决方法按时间戳留档。
- 发现：原 `复现进度.md` 记录了状态和结果，但对判断演变、错误假设和修正过程记录不足。
- 处理：新增本日志；历史条目标注“具体时间未留存”；以后每轮实验先追加日志，再更新进度摘要。
- 当前待测：Q2I-only、IGR、IGR+Q2I，seed=17，20K train、500 validation、2 epochs；关注 `repeat_recall` 和 `repeat_eligible`。
- 结论：`[流程修正完成]`。

### [2026-08-25 具体时间未留存] E013：三随机种子四组消融

- 配置：seeds=`[17,23,41]`，每组20,000 train、200 validation、2 epochs，matched cohort。
- 聚合输出：

| Variant | HR@1 | HR@5 | MRR | NDCG | NTP loss |
|---|---:|---:|---:|---:|---:|
| Base | 0.0600±0.0180 | 0.0833±0.0058 | 0.0673±0.0148 | 0.0713±0.0125 | 4.2219±0.0125 |
| Instruction | 0.0633±0.0236 | 0.0933±0.0284 | 0.0731±0.0249 | 0.0780±0.0258 | 4.2251±0.0128 |
| IGR | 0.0450±0.0260 | 0.0733±0.0362 | 0.0576±0.0297 | 0.0616±0.0313 | 4.2443±0.0067 |
| IGR+Q2I | 0.0417±0.0289 | 0.0767±0.0379 | 0.0559±0.0322 | 0.0612±0.0335 | 4.2448±0.0072 |

- 发现：Instruction 均值小幅高于 Base，但差值小于标准差；IGR 两组均值下降且方差增大。
- 判断：不能宣称 Instruction 稳定提升；当前公开代理 IGR 没有产生收益。`igr` 无 Q2I 监督，离散 top-k 又阻断 NTP 到检索分数的梯度，因此其 adapter 接近未训练检索器。
- 处理：增加 Q2I-only 变体；增加 SID-level repeat-recall 与 eligible count，直接诊断检索器是否取回长历史中的重复目标。
- 结论：`[负结果-有效诊断]`，不通过调参或只报最好种子掩盖。

### [2026-08-25 具体时间未留存] E012：联合损失符号与日志复核

- 输出截图确认：`train_loss=5.650157`，`ntp_loss=5.665108`，`q2i_loss=-0.074755`，`q2i_weight=0.2`，batch error=`0`，epoch error=`5.877e-08`。
- 发现：实际 Q2I 带负号，之前仅是文字转录遗漏。
- 判断：`5.665108 + 0.2×(-0.074755)=5.650157`；模型、权重和日志聚合均正确。
- 处理：解除 E011 的“待审计”状态；保留 identity assertion 防止未来回归。
- 结论：`[通过-实测]`。经验：遇到聚合算术矛盾先加机器断言，不依据手工转录猜测源码错误。

### [2026-08-25 具体时间未留存] E011：四组20K单种子消融

- 配置：seed=17，20,000 train、50 validation、2 epochs，matched cohort。
- 第2 epoch：Base `HR@5=0.06/MRR=0.045`；Instruction `0.08/0.060`；IGR `0.08/0.049`；IGR+Q2I `0.08/0.049`；合法SID均为1。
- 发现：日志文字中 Q2I 负号一度遗漏，导致表面上联合 loss 算术不一致。
- 初始判断：可能存在服务器源码、损失符号或日志聚合漂移，因此没有直接接受结果。
- 处理：加入逐 batch 和 epoch 两层 loss identity assertion，并用512样本复核。
- 结论：指标样本过少，只能作为中等规模流程测试。

### [2026-08-24 具体时间未留存] E010：真实数据 IGR+Q2I 5K Smoke Test

- 配置：5,000 train、20 validation、1 epoch、short=20、long=50、top-k=5。
- 输出：`train_loss=5.139547`，`ntp=5.235295`，`q2i=-0.478739`，HR@1/5与MRR均为0，`legal_sid_rate=1.0`。
- 发现：`5.235295 + 0.2×(-0.478739)=5.139547`，联合损失关系正确。
- 判断：20条验证样本且只训1 epoch时零命中不能用于方法判断。
- 处理：建立 matched cohort 的 Base/Instruction/IGR/IGR+Q2I 消融。
- 结论：`[通过-真实数据链路]`。


### [2026-08-24 具体时间未留存] E009：Instruction + Q2I + IGR 合成 CUDA 闭环

- 目的：一次验证双 instruction token、Q2I 联合损失、长历史 top-k、padding mask 和 backward。
- 输出：`joint_loss=2.914326 -> -0.162442`，`ntp=0.024518`，`q2i=-0.934801`，`igr_shape=(4,2)`，`instruction_delta=0.206740`。
- 发现：联合 loss 可下降；instruction 改变 logits；IGR 未选 padding。
- 判断：负 Q2I 来自负余弦主项，属于预期；该实验只证明控制流，不证明推荐收益。
- 处理：构造真实数据无重叠 short/long history batch。
- 结论：`[通过-合成实测]`。

### [2026-08-24 具体时间未留存] E008：RQ 2×2 冻结实验

- 关键输出：
  - width=256/K-Means++：`mse=0.001678`，`collision=0.203331`，`unique_sids=16449`，prefix=`[256,7434,16449]`；
  - width=512/random：`mse=0.001584`，`collision=0.325148`，`unique_sids=15001`，prefix=`[512,11890,15001]`。
- 发现：width=512/random 的 MSE 略优，但完整 SID 基数和碰撞更差。
- 判断：推荐 tokenizer 应优先降低 item collision、提高可区分 SID 数，而非追求极小 MSE 差异。
- 处理：冻结 width=256/K-Means++、seed=17 作为 Phase-2 公开代理 registry。
- 结论：`[通过-代理冻结]`；碰撞仍有20.33%，后续必须作为风险报告。

### [2026-08-24 具体时间未留存] E007：width=512/K-Means++ 异常碰撞

- 输出：`represented_items=18733`，`unique_vector_rate=1.0`，`mse=0.003906 -> 0.001596`，`collision_rate=0.444243`。
- 发现：width 增大、MSE 下降，但 collision 反而恶化。
- 判断：K-Means 优化重构误差，不直接优化完整 SID 唯一性；不能只按 MSE 选 tokenizer。
- 处理：补齐 width=256/K-Means++ 与 width=512/random 的2×2控制实验。
- 结论：`[负结果-有解释]`，停止无边界地继续调普通 K-Means。

### [2026-08-24 具体时间未留存] E006：属性代理 RQ width=256

- 输出：`represented_items=18733`，`unique_vector_rate=1.0`，`mse=0.003906 -> 0.001671`，`collision_rate=0.211071`。
- 发现：输入向量没有完全重复；扩大码本显著降低碰撞。
- 判断：剩余碰撞主要来自 RQ 容量与聚类目标，而非完全相同的输入向量。
- 处理：比较 width、初始化、MSE、collision、unique SID 和 prefix occupancy。
- 结论：`[候选可用-实测]`。

### [2026-08-24 具体时间未留存] E005：属性代理 RQ width=64

- 配置：RetailRocket 训练截止前最新属性，256维 signed hashing，width=64。
- 输出：`represented_items=4808`，`mse=0.003906 -> 0.002105`，`collision_rate=0.911606`。
- 发现：接口和训练正常，但91.16%的 item 落入碰撞 SID。
- 判断：该 registry 不可用于后续推荐实验。
- 处理：扩大到 width=256，并确认输入向量是否重复。
- 结论：`[失败-质量门槛]`，实现未失败，SID 唯一性失败。

### [2026-08-24 具体时间未留存] E004：合成向量 CUDA Residual K-Means

- 目的：验证三层分块 PyTorch RQ 实现。
- 输出：`shape=(3,8,16)`，`mse=0.927709 -> 0.006996`，`collision_rate=0.884766`。
- 发现：MSE 显著下降，但 width=8 fixture 碰撞很高。
- 判断：高碰撞由测试容量过小导致，不能据此否定 RQ 实现。
- 处理：转到真实属性代理向量并扩大 width。
- 结论：`[通过-实测，仅正确性 fixture]`。

### [2026-08-24 具体时间未留存] E003：RetailRocket 100K 基础训练

- 目的：验证真实数据读取、时间切分、样本构造、训练、评测和 checkpoint 全链路。
- 配置：100,000 训练样本，3 epochs，单卡 L20；当时使用 frequency-bootstrap SID。
- 输出：3 个 epoch 正常打印，train loss 正常下降；具体数值未因信息安全要求导出。
- 发现：真实数据训练链路可稳定运行。
- 判断：只能证明工程闭环，frequency-bootstrap 不是语义 tokenizer。
- 处理：实现属性代理向量和 CUDA Residual K-Means。
- 结论：`[通过-实测，结构代理]`。

### [2026-08-24 具体时间未留存] E002：Toy CUDA Beam Search

- 目的：验证多候选合法路径和 beam 排序。
- 输出：`[[[1,2,3],[7,8,9]], [[7,8,9],[1,2,3]]]`。
- 发现：每个 beam 都是 registry 中的完整 SID，排序控制流正常。
- 判断：reference beam search 具备正确性，但未优化吞吐和 KV cache。
- 处理：接入真实 RetailRocket 样本。
- 结论：`[通过-实测]`。

### [2026-08-24 具体时间未留存] E001：Toy CUDA 训练与约束生成

- 目的：验证 encoder-decoder forward/backward、单 batch 拟合和合法 SID 生成闭环。
- 配置：CUDA toy batch，三层 SID，PrefixTrie constrained greedy。
- 输出：`loss=2.312395 -> 0.002499`，生成 `[[1,2,3],[7,8,9]]`。
- 发现：loss 可收敛，生成路径均在 trie 中。
- 判断：基础模型、损失、优化器和 greedy 约束控制流正确。
- 处理：进入 beam search 验证。
- 结论：`[通过-实测]`，不能代表真实数据推荐效果。


## 新实验追加模板

```markdown
### [YYYY-MM-DD HH:MM:SS TZ] EXXX：实验名称

- 目的：
- 配置：
- 输出：
- 发现：
- 判断：
- 处理：
- 结论：
- 下一步：
```
