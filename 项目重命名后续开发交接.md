# OxygenREC 复现项目重命名交接

更新时间：2026-08-21  
重命名前目录：`/Users/coffeetau/projects/HuaWei/OxygenREC`  
交接目的：目录改名并重新打开 Codex 后，直接恢复当前实现进度。

## 1. 重新打开后的第一条指令

建议在新目录中对 Codex 说：

> 请先完整阅读 `项目重命名后续开发交接.md`、`README.md`、
> `configs/assumptions.yaml` 和 `OxygenREC复现项目交接.md`，检查当前目录与测试，
> 然后从“最小 PyTorch encoder-decoder”继续。不要重新调研或重写已经完成的 SID、
> 数据切分和量化模块。

## 2. 项目目标与准确表述

目标是：

> OxygenREC 论文方法自实现 + 公开数据近似 benchmark + GPU/NPU 迁移与性能评测。

不是：

- 京东生产模型严格复现；
- 京东私有数据主表复现；
- 论文线上 UCTR、GMV 等收益复现。

原因是论文没有公开完整工业数据、特征体系、训练代码、checkpoint、统一 ranking
reward service 和线上流量协议。

实现顺序保持不变：

```text
小型 dense OxygenREC-v1 核心闭环
  -> instruction fusion
  -> Q2I + IGR
  -> 多行为/多场景训练
  -> 后训练与 SA-GCPO
  -> 规模化和 MoE
  -> GPU/NPU 对齐
```

不要直接从 3B MoE 或 RL 开始。

## 3. 论文与版本边界

工作目录中的论文：

`Hao 等 - 2025 - OxygenREC An Instruction-Following Generative Framework for E-commerce Recommendation.pdf`

- 论文：OxygenREC 原版，即当前实现的第一阶段目标；
- SHA-256：`ea9b888860e562bc54e8fe8d787c13a79ee8ff0ca5c9019b0fbe193257b9e003`；
- SID 论文事实：256 维商品表示，三层 RQ-KMeans，每层宽度 8192；
- 原版 scaling ablation 包含 0.7B total / 0.4B active；
- 不要把它误写成 OxygenREC-v2 离线主表规模；
- v2 离线研究使用 3B encoder-decoder，线上为 3B total / 1B active MoE。

完整论文分析和 v1/v2 边界见 `OxygenREC复现项目交接.md`。

## 4. 当前已完成内容

### 4.1 公开代码复用审查

详见 `docs/reference_reuse.md`。

- MiniOneRec：Apache-2.0，可参考 RQ-KMeans、约束解码、指标和 RL；
- OpenOneRec：Apache-2.0，后续参考 instruction benchmark 与 veRL 管线；
- GRID：许可证限非商业研究，只阅读算法，不复制源码；
- 没有找到 JD 官方 OxygenREC 模型训练代码。

当前代码是 clean-room 实现，没有把上述仓库复制进项目。

### 4.2 SID 基础设施

文件：`src/oxygenrec/sid.py`

已实现：

- 不可变 `SemanticID`；
- 默认三层、每层范围校验；
- 版本化 `SIDRegistry`；
- item -> SID 和 SID -> items；
- collision 显式保留；
- JSON 保存/加载；
- `PrefixTrie` 合法前缀与 next-code 查询。

重要决策：论文没有披露 collision resolution，因此当前不自动添加第四层去重 token，
而是报告并保留冲突。

### 4.3 数据层

文件：

- `src/oxygenrec/data/events.py`
- `src/oxygenrec/data/temporal.py`
- `docs/data_protocol.md`

已实现：

- 统一的 `view/addtocart/transaction` 事件 schema；
- RetailRocket `events.csv` 流式适配器；
- 全局 train/validation/test 时间边界；
- train-only 商品词表；
- 严格早于 target 的 history；
- 同毫秒事件互相不可见；
- validation/test history 可使用更早 split 的历史；
- 冷启动 target 默认与普通 in-vocabulary benchmark 分离；
- history 截断保留最近事件。

RetailRocket 数据卡许可证为 CC BY-NC-SA 4.0，因此它目前只是可选研究适配器，
没有自动下载或写死为正式企业 benchmark。数据核心保持 dataset-neutral。

### 4.4 Residual K-Means 和 SID 指标

文件：

- `src/oxygenrec/quantization.py`
- `src/oxygenrec/sid_metrics.py`
- `docs/sid_protocol.md`

已实现：

- 纯 Python、固定 seed 的 K-Means++；
- Lloyd K-Means；
- 逐层 residual quantization；
- SID encode 和 embedding reconstruct；
- 自动生成 `SIDRegistry`；
- codebook JSON 保存/加载；
- codebook version 和 shape 校验；
- depth 1/2/3 prefix coverage；
- SID collision P90/P99/P99.9；
- colliding-item rate；
- 每层 codebook load balance。

这个实现只作为小数据确定性 reference，不用于几十万商品。正式后端应使用
FAISS/PyTorch，并先在固定 fixture 上和 reference 做 parity。

### 4.5 L20 服务器环境采集

文件：

- `scripts/collect_server_env.py`
- `docs/server_environment.md`

用户下一步会把脚本传到 8×L20 服务器，在实际训练 Python 环境中运行：

```bash
python collect_server_env.py --output oxygenrec_server_env.json
```

重新打开 Codex 后，优先询问用户是否已经拿到 `oxygenrec_server_env.json`。
拿到后根据实际 Python/PyTorch/CUDA/cuDNN/NCCL 版本确定：

- 本地 CPU PyTorch 版本；
- 服务器 CUDA PyTorch 版本；
- `requirements/common.txt`；
- `requirements/local-cpu.txt`；
- `requirements/l20-cuda.txt`。

不要覆盖服务器已经安装并验证的 PyTorch 环境。

## 5. 当前测试状态

当前环境：macOS arm64、Python 3.14，本地尚未安装 PyTorch。

验证命令：

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v
```

交接前结果：

```text
Ran 18 tests in 0.002s
OK
```

测试文件：

- `tests/test_sid.py`；
- `tests/test_data.py`；
- `tests/test_quantization.py`。

重命名后首先重新运行这条命令。目录重命名不应影响这些测试，因为代码没有硬编码
旧项目绝对路径。

## 6. 下一步准确任务

### 情况 A：用户已经提供服务器环境 JSON

1. 读取并总结服务器 Python、PyTorch、CUDA、cuDNN、NCCL、GPU topology；
2. 检查 8 张卡是否均为 L20、显存是否一致、NCCL 是否可用；
3. 固定本地/服务器依赖文件；
4. 本地使用 CPU wheel，不安装 CUDA wheel；
5. 开始 PyTorch 模型实现。

### 情况 B：服务器环境 JSON 还没拿到

不要阻塞模型结构设计。先写设备无关的小型 PyTorch 模块接口，但不要拍脑袋固定
服务器 CUDA wheel。可以等待环境信息后再安装本地 torch。

最小 PyTorch 模型按以下顺序实现：

```text
history item IDs
  -> SIDRegistry 查 SID
  -> level-aware SID embeddings
  -> encoder input + padding mask
  -> dense Transformer encoder
  -> BOS + default instruction token
  -> Transformer decoder + cross attention
  -> 三个 level-specific prediction heads
  -> weighted NTP loss
```

第一版不实现：

- 多模态 Qwen3+CLIP+Q-Former；
- Q2I/IGR；
- MoE；
- SA-GCPO；
- FlashAttention；
- 多卡 DDP。

第一版验收：

1. 固定 toy batch 的 logits shape 正确；
2. padding/causal/cross-attention mask 正确；
3. loss 能 backward；
4. 单 batch 能过拟合；
5. checkpoint 保存恢复后 logits 对齐；
6. 生成的 SID 能被 `PrefixTrie` 约束为合法路径。

## 7. 重命名注意事项

### 只改目录名时

内部 Python package `oxygenrec` 建议暂时不要跟着改。它代表论文方法名，不依赖项目
外层目录名。保持以下 import：

```python
from oxygenrec import SIDRegistry
```

重新打开 Codex 时，确认工作目录已经是新的绝对路径。

### 如果还要改 Python package 名

这属于第二个独立改动，应在重命名后单独执行并全局搜索：

```bash
rg -n "oxygenrec|OxygenREC|/Users/coffeetau/projects/HuaWei/OxygenREC" .
```

注意区分：

- 项目品牌名：可以改；
- Python package 名：可以改，但会影响 import；
- 论文/模型名 OxygenREC：不应改；
- 旧绝对目录：必须检查并替换；
- `oxygenrec_server_env.json` 文件名：只是报告名，可不改。

### 当前 Git 状态

交接时目录不是 Git repository，没有 `.git`。重命名后不要假设可以使用 `git diff`
或 `git status`。如果准备初始化版本控制，应作为显式步骤执行，并在首次 commit 前检查
PDF、数据、checkpoint 和环境报告是否应该纳入版本控制。

当前 `.gitignore` 已忽略：

- Python cache；
- virtual environment；
- `artifacts/`；
- raw/processed data；
- checkpoints。

论文 PDF 当前没有被忽略；初始化 Git 前由用户决定是否提交。

## 8. 文件导航

```text
README.md                         当前总体进度
项目重命名后续开发交接.md          本文
OxygenREC复现项目交接.md           完整长期路线与 v1/v2 边界
configs/assumptions.yaml          论文事实、实现假设、未知参数
docs/reference_reuse.md           公开项目/许可证/迁移矩阵
docs/data_protocol.md             数据字段与无泄漏切分
docs/sid_protocol.md              SID 构建与指标
docs/server_environment.md        L20 环境采集说明
scripts/collect_server_env.py     服务器环境采集脚本
src/oxygenrec/sid.py              SID registry 和 prefix trie
src/oxygenrec/data/               数据 schema 与时间样本
src/oxygenrec/quantization.py     reference residual K-means
src/oxygenrec/sid_metrics.py      SID 诊断指标
tests/                            当前 18 项测试
```

## 9. 恢复工作检查清单

重新打开 Codex 后按顺序执行：

```text
[ ] pwd，确认新项目目录
[ ] rg --files，确认文件完整
[ ] 阅读本文和 assumptions.yaml
[ ] 运行 18 项测试
[ ] 搜索旧绝对路径
[ ] 询问/读取 oxygenrec_server_env.json
[ ] 固定 PyTorch 版本和依赖文件
[ ] 实现最小 dense encoder-decoder
[ ] toy batch forward/backward/overfit
[ ] 接 PrefixTrie 约束解码
```

## 10. 不要重复或误做的事情

- 不要重新实现已经存在的 SID registry、时间切分和 reference RQ-KMeans；
- 不要把 RetailRocket 自动下载写进默认流程；
- 不要把 GRID 非商业许可证代码复制进项目；
- 不要声称公开数据结果复现了京东主表；
- 不要在本地 macOS 安装 CUDA wheel；
- 不要在服务器环境信息未知时强行覆盖其 PyTorch；
- 不要在 dense toy 闭环前开始 3B MoE 或 RL。

